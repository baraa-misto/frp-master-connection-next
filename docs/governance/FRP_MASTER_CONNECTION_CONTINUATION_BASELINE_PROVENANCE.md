# FRP Master Connection — Continuation Baseline Provenance

## Purpose

This repository is the clean continuation repository for FRP Master Connection development.

It was intentionally started with a fresh Git history while preserving the exact tracked project snapshot from the prior repository.

## Source repository

- Repository: `https://github.com/baraa-misto/frp-master-connection.git`
- Source commit: `5a391ce68cb04fba3df20f5bd3b57a01fd05da19`
- Source commit count in prior repository: `139`

The prior repository remains the authoritative historical archive for all pre-rollover commits, tags, freezes, and milestone provenance.

## Source freeze history

At rollover, the prior repository contained **18 existing annotated freeze tags**. Those historical tags remain in the prior repository and are intentionally **not imported** into this continuation repository.

Do not recreate historical freeze tags here as if they originated in the new history.

## DCTN status at rollover

The exported source snapshot includes the current DCTN-3B implementation and UI refinement present at source commit `5a391ce68cb04fba3df20f5bd3b57a01fd05da19`.

At rollover:

- DCTN-3B implementation/refinement had passed local and fresh object-isolated QA;
- hosted GitHub Actions jobs were blocked before startup by an account payment/spending-limit restriction;
- DCTN-3B had **not** completed independent acceptance;
- DCTN-3B had **not** been frozen;
- no DCTN freeze tag existed in the source repository.

The continuation repository must not misrepresent this as a frozen/accepted milestone.

## Continuation policy

Future work occurs in this repository.

The prior repository should remain unchanged except for optional GitHub archival/read-only administration by the owner.

Historical engineering authority remains traceable through:
1. this provenance record;
2. the recorded source commit above;
3. the archived prior repository and its original freeze tags.

## Baseline verification

The rollover procedure must verify that every tracked file copied from the source commit has identical Git blob content in the new baseline commit, excluding this provenance file and any explicitly authorized continuation-only governance file.

The new repository's first commit SHA is recorded after creation in the rollover completion report.

END OF CONTINUATION BASELINE PROVENANCE
