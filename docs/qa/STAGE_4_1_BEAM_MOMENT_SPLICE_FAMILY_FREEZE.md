# Stage 4.1 Beam Moment Splice Family Freeze

## Authority and accepted baseline

Governance/reproducibility only. The complete external controlling order was read
before repository action. SHA-256 is
`97C9F8EE4A22B57EB68B0217566D948204CB76B967858B4E0C98FDB7B5FE16F7`; its final
Stage 4.1 family-freeze sentinel is present. The clean synchronized starting and
accepted product baseline is `6d953dbef648bd35ab21b5208432579cdeb59902`, count 102.

The six-commit chain is reachable: Slice 5 `23813a5...`, 4.1A `59713e5...`, 4.1A
freeze `cc9effa...`, Slice 6 `b275a64...`, 4.1B `bd0529e...`, and R1 `6d953db...`.
The controlling order records accepted four-job runs #93 (6m05), #94 (6m01), #95
attempt 6 (5m56), #96 (5m52), and #97 (5m27). Slice 5 #92 (5m26) is retained from
the hash-verified prior W/I freeze record. These are owner-controlled predecessor
acceptance records, not a claim of newly retrieved hosted evidence. Owner visual
acceptance is complete for W/I and R1-corrected Channel per this order, including
combined/pure/reversed actions, topology/X-ray/hardware, reference/eccentricity,
explicit design, and rational review. The new governance commit's CI remains pending
direct Backend/Frontend Ubuntu/Windows evidence after publication.

## Immutable scope and pre-mutation audit

The family freezes `CS5-RC1`, `4.1A-RC1`, `CS6-RC1`, `4.1B-RC1`, and the R1 runtime
correction. All 16 controlled artifacts, six external predecessor orders, and 53
inherited authorities were hash-verified before mutation. G ranges are exactly
52/96/72/112. Eight existing annotated tag objects and peeled remote/local targets
matched. PDFs were neither copied nor added. Existing freeze manifests/audits stay
byte-identical. Shared material, geometry/path, demand, Chapter 7, resistance-handoff,
status, and preview-state architecture remains unchanged.

The accepted backend-default W/I fingerprints match the old W/I freeze exactly.
Channel canonical, geometry, engineering, application, component, and design identities
were sampled from accepted public services and pinned. Existing frontend-wire R1
U.S./SI fingerprints are separately labeled, not substituted for backend-default inputs.

Protected trees:

- `backend/src`: `9ae60f41686819cfe23e9b092d7516f33cd7a557`.
- `frontend/src`: `18f335ef899d501a7a8a66beae2fd513d66149a6`.
- `.github/workflows`: `20edd491455e6329c4565bc72cd7b88e29d3f7b1`.
- `backend/requirements`: `d8e1a9f1c2c89a4e50a181db1997c52b3c9bdf58`.
- `docs/engineering`: `c51641d6799b426e366631e68f7ec1b3bbf7cf6d`.
- `backend/tests/golden`: `ca5bb519aeec22099dd7ed3084d6cfe24fe06e31`.

Full package/lock/blob SHA-256 identities, source leaves, controlled/inherited records,
and tag targets are in the manifest. Production, methods, controlled engineering
artifacts, dependencies, lockfiles, workflows, and existing-tag changes must equal zero.

## Manifest and successor-safe audit

Manifest: `docs/governance/STAGE_4_1_BEAM_MOMENT_SPLICE_FAMILY_FREEZE_MANIFEST.json`.
Final immutable SHA-256:
`2C79F811EECFD89C04862359E583A240284872C10A0603E3D6CA276D657CED77`.
Audit: `backend/tests/calculation/test_stage_4_1_beam_moment_splice_family_freeze_manifest.py`.

The audit resolves an annotated family tag, an explicitly supplied historical full
commit SHA, or `manifest_only`. It never infers successor HEAD as the frozen object.
Raw commit headers prove the parent at depth one. Pinned path/mode/blob inventories
reconstruct Git tree identities; available historical blobs are hash-verified. Missing
history retains authenticated manifest identities, not working-tree source. No network
or Git alternates are used. A separate accepted-candidate gate is deliberately bounded
to the product baseline or correctly parented/subjected governance candidate.

Tamper-negative tests cover every order section 34 category, including source
addition/deletion/rename/content, dependencies/workflows/artifacts, baseline/R1,
topologies, Channel references/torsion/ledger/face split, W/I split inners, force lines,
common-bolt planes, invented capacity, review/qualification/source boundaries,
fingerprints, blank-root invariant, wrong objects/tags, and successor substitution.
Enumeration and Windows separators reconstruct identical trees.

## Local QA run

Complete local integrated QA passed. Backend: 2,950 tests; 24,142 statements and 6,556
branches covered at 100%; Ruff format/lint, strict mypy (240 files), hash-locked install,
editable package, pip consistency, Uvicorn/Alembic CLI, runtime imports, and psycopg
binary smoke all passed. The final backend rerun after hardening the new test's
missing-history negative case passed in 111.63 seconds. Focused new audit: 54 tests.

Frontend: 603 tests in 42 files; 4,666 statements, 3,965 branches, 1,648 functions,
and 3,385 lines all covered at 100%; locked install, dependency-tree validation,
ESLint, strict TypeScript, production build, full audit, and runtime-only audit passed.
Both audits reported zero vulnerabilities. Runtime versions were Python 3.14.6,
Node 24.18.0, and npm 11.16.0. All 54 tracked/new JSON files validated; whitespace
passed. Complete suites include all four golden matrices, R1 selector/direct-load/runtime
regressions, historical product fingerprints, and every historical freeze audit.

The independent pre-commit integrity check authenticated the finalized manifest,
six protected source/authority trees, 74 package/controlled/inherited records, all
eight existing tag identities, and exactly 11 authorized changed paths. It confirmed
zero protected source, artifact, dependency, lockfile, workflow, or old-tag changes.
Only the new freeze test/manifest/QA record and eight narrow governance documents
are included; no frontend file is changed.

## Post-commit verification and publication contract

Commit subject: `chore: freeze Stage 4.1 beam moment splice family baseline`.
Expected count: 103. No amendment is authorized. The commit cannot record its own
unknown SHA or post-commit outcomes without violating the one-commit rule; direct
post-commit evidence belongs in the final completion report.

Before publication, require fresh exact depth-one/no-tags/no-alternates full QA,
manifest-only and explicit-object audit modes, actual future-style tagless successor
simulation, exact protected identities, and clean worktree. Stop on failure. Only then
push main normally without tags and verify local/origin/remote equality. Create only
annotated `stage-4.1-beam-moment-splice-family-freeze` with annotation
`Freeze accepted Stage 4.1 Beam Moment Splice Family baseline`; require post-tag
`source='tag'`, rerun all historical freeze audits, then push that exact tag only.
All eight previous tag objects/targets must remain exact. New hosted four-job CI must
be observed directly; a pending or infrastructure-failed run authorizes no source fix.

No next moment-connection family is begun or authorized by this freeze.
