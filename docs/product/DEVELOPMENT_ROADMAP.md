# Development Roadmap

## DCTN-3B accepted continuation milestone

Continuation implementation `d556585c1b905532431e4ec9b7be9ab23acabc10` passed complete isolated QA, exact-SHA hosted CI, independent browser/runtime review, and read-only acceptance. [The frozen scope](../governance/DCTN_3B_CONTINUATION_FREEZE.md) closes DCTN-3B RC1 relative placement, P/Qp/Qq demand transport, historical P-only response, physical shaft ownership, status/presentation behavior, and the explicit unqualified transverse-response boundary. It does not implement automatic transverse sharing/contact/prying/resistance, add a route/body/material selector, or authorize later DCTN engineering. The tag targets the accepted implementation; this governance successor must pass fresh QA and its own exact-SHA hosted CI.

## CME-3 accepted conditional public activation milestone

Implementation `d790aacf643a265a839f8ae39051e70915fe5d54` passed full QA, exact-SHA four-job CI and read-only product acceptance. [Frozen scope](../governance/CME_3_316SS_PUBLIC_ACTIVATION_FREEZE.md) closes conditional connector-body selection on thirteen routes, not universal stainless capacity or complete source qualification. Unknown response/contact/prying, unqualified shape/stability/local mechanisms, unsupported C2 applicability and external hardware/foundation design remain explicit. No later engineering-method expansion is authorized; historical exclusions below describe their original milestones. The tag targets implementation, while the governance successor must pass fresh QA and its own hosted CI.

## CME-2 core accepted isolated milestone

C2-R/A/T implementation `1d730c70bc446901452f2eacecd9df3f9f36ff4b` passed full QA, exact-SHA CI and independent read-only acceptance. [Freeze scope](../governance/CME_2_CORE_R_A_T_FREEZE.md) targets the implementation, not its governance successor. This closes only the isolated bounded providers; it does not complete public stainless integration, qualify unknown contact/prying response or authorize CME-3. Further work requires separate authority.

## CME-2 core internal provider milestone

C2-R/A/T isolated implementation follows frozen C2-P2. [Current scope](../architecture/CME_2_CORE_ISOLATED_RESPONSE_ANGLE_TEE.md) does not activate any of the sixteen public families or thirty-nine connector bodies. Implementation QA/CI, independent read-only acceptance and conditional freeze are sequential gates. Public stainless integration, contact-solving and CME-3 require later authority.

## CME-2C-F current milestone

The isolated C2-P2 implementation `61855bda1f76bbf11a85ae58664ed81bbc8ea419` is accepted for the scope recorded in [the freeze record](../governance/CME_2C_C2_P2_FREEZE.md). Its annotated tag targets that implementation; the governance successor records acceptance without changing calculations.

After the freeze fully passes, a read-only combined C2-R response / C2-A angle / C2-T Tee readiness audit may map methods, fabrication/source gaps and proposed test authority. Numerical implementation, provider/family activation and CME-3 require separate approval. Isolated C2-P2 acceptance supplies no common-fastener, contact/prying, material-dependent redistribution or whole-connection stainless qualification.

## CME-2B-F current milestone

The accepted isolated C2-M/C2-P1 implementation `7ef49bdbf8b9704af3d314735963069ddf7259f3` is the target of `cme-2b-c2-m-c2-p1-freeze`; [freeze boundaries](../governance/CME_2B_C2_M_C2_P1_FREEZE.md) control this milestone. The material/provider foundation and historical FRP families remain unchanged.

C2-P2, C2-R, C2-A, C2-T, CME-3 and public stainless family activation remain excluded and require separate controlling approval. No response redistribution, common-fastener sharing, contact/prying, welding or complete stainless connection approval follows from isolated plate acceptance. The overall product is not globally frozen.


## CME-2B RC1 R1 — isolated numerical successor

The [C2-M/C2-P1 internal material/flat-plate provider](../architecture/CME_2B_ISOLATED_STAINLESS_PLATE_PROVIDER.md) is implemented, subject to its full local/hosted publication gates. CME-1 remains frozen and public SS316 family readiness is unchanged. C2-R response, C2-A angles, C2-T Tees, C2-P2 stability/combined action and CME-3 family/UI activation require separate authority. No existing family is enabled by this implementation and the product remains development/draft. Earlier milestone descriptions below retain their historical meaning.

## CME-1F current milestone

CME-1 foundation is frozen at `d62064a7d96ba139841c222f90b62b8e7c4601c8` under `cme-1-connector-material-extension-freeze`; Stage 4.5 remains frozen at `998911ea78872b852ef097dd4e20d666a33a935a`. [Freeze scope and compatibility rules](../governance/CME_1_CONNECTOR_MATERIAL_EXTENSION_FREEZE.md) control this milestone; earlier future-stage statements are historical.

Next: CME-2 actual SS316 numerical provider/method implementation through the frozen material/provider interfaces, requiring separate source authority and owner approval after governance CI review. No stainless numerical work is completed or started here. The overall product remains in active development.


## Stage 4.4 RC1 current successor

`ANGLE_COLUMN_TWO_LEG_MOMENT_BASE_CONNECTION` / `4.4-RC1` starts from
`1faa1ff522d0e0a39a42e2dc2d3974b5dba98479` (114 commits). Equal and unequal
FRP Angle columns each have exactly two independent FRP base angles, one on each
different leg, with actual member bolts and separate foundation attachment groups.

One new product uses the accepted Angle geometry, material-neutral Slice 7 core/FRP provider and Slice 8 native in-plane demand. Source-limited branch allocation remains explicit; total required foundation actions are available without invented connector or anchor response. Historical frozen families are unchanged.

See [native integration/source boundaries](../engineering/STAGE_4_4_NATIVE_INTEGRATION_AND_SOURCE_BOUNDARIES.md)
and [96-requirement QA catalogue](../qa/STAGE_4_4_ANGLE_COLUMN_MOMENT_BASE.md).
The exact implementation subject is `feat: add angle-column two-leg moment base connection`;
normal expected count 115. Local, browser and fresh object-isolated QA precede the
normal non-force main push; direct four-job CI follows it. Future CI is not inferred.
All eleven existing freeze tags remain immutable. Owner final visual/result acceptance
is PENDING; no Stage 4.4 freeze, Stage 4.5 or 316SS expansion is authorized.

## Roadmap controls

This roadmap orders capability development; it does not approve an engineering method, a code interpretation, a calculation, or commercial use. A stage or slice advances only after its applicable governance and validation gates are satisfied. Scope may be refined through the Decision Register without silently reopening approved baseline decisions.

## Stage 0 — Controlled foundation

### Stage 0.1 — Repository and controlled-document foundation

Status: Complete and committed at baseline commit `9cf4781256870d307feb24d654608201998e35f5`.

- Establish the separate Git repository and proprietary-source boundaries.
- Record the product baseline, stable approved decisions, terminology, artifact versions, architecture boundaries, engineering-source controls, risk register, QA plan, security model, and handoff manifest.
- Register ASCE/SEI 74-23 bibliographic and control metadata only, with errata verification pending.
- Establish fail-closed result semantics, canonical-model principles, reproducibility expectations, and SaaS-ready boundaries.
- Keep all architecture recommendations provisional until their decision entries are approved.
- Implement no production code, equations, capacities, numerical code criteria, utilization logic, optimization, or report generation.

### Stage 0.2.1 — Toolchain and dependency compatibility preflight

Status: Complete, reviewed, and committed at `4dc8a8b1ec0f5198d9deb5479f14b89ccf956359`. Compatibility evidence is accepted for implementation planning as a provisional, not-frozen baseline; final production toolchain approval remains pending.

- Test the proposed backend and frontend runtimes, packages, development tools, and dependency trees in isolated disposable environments before repository scaffolding.
- Record exact candidates, declared runtime and peer constraints, commands, outcomes, warnings, security-audit observations, alternatives, and risks in `../architecture/TOOLCHAIN_AND_DEPENDENCY_COMPATIBILITY.md`.
- Create no permanent dependency manifest, lockfile, application source, production configuration, engineering equation, capacity, numerical criterion, or calculation behavior.

### Stage 0.2.2 — Permanent backend toolchain and API shell

Status: Complete, reviewed, and committed at `f31940ea232ade6df935f5612393a7126fa56e30`. The Stage 0.2.1 evidence was used to create the provisional Windows/CPython 3.14 backend package, hash-bearing development lock, two-route FastAPI service shell, trusted local identity boundary, fail-closed production composition, and backend QA foundation. The production toolchain and deployment are not approved or frozen, and engineering calculation approval is not applicable because no calculation exists.

### Stage 0.2.3 — Permanent frontend toolchain and application shell

Status: Complete and reviewed. Controlled validation passed, and the exact Node 24.18.0/npm 11.16.0 baseline, private unversioned React/TypeScript package, authoritative npm lock, strict quality gates, and accessible noncalculating shell are provisionally accepted as the frontend application foundation. This is not a production or cross-platform toolchain freeze. Three.js and React Three Fiber remain deferred until separately authorized canonical-geometry and visualization work.

The shell exposes exactly the two approved primary categories and nonfunctional project, geometry, force, result, 3D, and orthographic-view placeholders. No persistence, force-entry, API-integration, engineering-geometry, visualization, calculation, or report implementation exists. The approved engineering-slice order below is unchanged.

### Stage 0.2.4 — Integrated repository QA and CI

Status: Implemented, pushed, and hosted-run confirmed. The run for commit
`954ab1286b70f41e817f011b5885d19cd4b43210` completed successfully in 1 minute 46
seconds with all four backend/frontend Ubuntu/Windows jobs successful and no
artifacts. This establishes software-foundation evidence only and does not freeze a
production or cross-platform toolchain.

### Remaining Stage 0 foundation work

- Resolve the minimum engineering and architecture decisions needed for implementation.
- Verify controlled engineering sources and errata.
- After the applicable approvals, create reviewed project scaffolding without coupling the engineering core to providers.
- Define versioned contracts, migrations, capability declarations, and validation evidence structures.

## Stage 1 — Engineering-domain foundation

### Stage 1.1 — Joint-assembly domain contracts

Status: Implemented, committed, pushed, and hosted-run confirmed at
`95f469907139a6bc1a365dd42675300384623347`. Four of four backend/frontend
Ubuntu/Windows jobs passed in 1 minute 32 seconds with no artifacts. The
standard-library-only domain
package defines immutable joint assemblies, members, connector components, supports,
interfaces, bolt groups and locations, factored-strength load-combination identities,
manual six-component member-end actions, unit-system identity, symbolic frames and
reference points, preliminary pultruded-FRP component material axes, and deterministic
validation issues.

This stage implements data contracts and integrity validation only. It contains no
engineering geometry, transformation, property value, equation, resistance, force
distribution, equilibrium result, utilization, visualization, persistence, API,
frontend integration, or report behavior.

### Stage 1.2 — Section topology and FRP material regions

Status: Implemented, committed, pushed, and hosted-run confirmed at
`9de91c81681ebd14e136dfd8ec3470b3a0c20d18`. The user verified all four
backend/frontend Ubuntu/Windows jobs green. The exact run duration and artifact
status were not supplied and remain unknown.

Stage 1.2 replaces the preliminary component-wide FRP LW/CW contract with one shared
component LW and explicit region CW/TT orientation rules. It implements immutable
physical section elements, material regions, standard and custom symbolic topology,
sign-insensitive material axis families, planar orientation, symbolic cylindrical
round-tube orientation, scoped deterministic validation, and explicit topology for
members and connectors. Standard definitions cover W and general I sections, channels,
tees, rectangular/square tubes, angles, plates/doublers, and round tubes.

This stage contains no exact geometry, material-property set, manufacturer data,
coordinate transform, interface/face/bolt targeting, directional property lookup,
equation, strength, resistance, capacity, utilization, persistence, API exposure,
frontend integration, visualization, or report behavior. Solid-round material rules
and generic junction/corner/fillet regions remain deferred.

### Stage 1.3A — Spatial mathematics and action direction contracts

Status: Implemented, locally validated, committed, pushed, and hosted-run confirmed at
`368d77f7de121f7de8f25f34bc30bfe7b939fcb1`. The user verified all four
backend/frontend Ubuntu 24.04/Windows 2025 jobs green. Exact duration and artifact
status were not supplied and remain unknown.

Stage 1.3A establishes the standard-library-only mathematical kernel for finite
geometric vectors and unit vectors, right-handed resolved Cartesian frames, explicit
member-frame construction, proper rigid transforms, local/parent conversion,
composition and inversion, force/moment rotation, explicit common-frame
reference-point shifting, connected-end axial interpretation, renderer-neutral
positive/applied action directions, and numerical frame inspection.

The global frame is right-handed with project-horizontal X/Y and vertically upward
+Z. Member +x is fixed START to END. A standard unrotated W/I frame uses +y along
flange width and +z along web depth. Frame construction requires an explicit local-z
reference, rejects degenerate or near-parallel inputs, and never chooses a fallback.
The centralized `1.0e-12` tolerance is Provisional, dimensionless, and limited to
mathematical unit/orthogonality/handedness/angular checks.

This stage resolves mathematical frames; it does not add exact section geometry,
member/connector/interface placement, a `JointAssembly` frame registry, frontend
rendering, an API endpoint, persistence, a material property, an engineering
equation, force distribution, resistance, capacity, utilization, or reports. Those
remain separately governed later stages. Stage 1.3A direction data is mandatory
input to future graphical verification but is not itself a visualization
implementation.

### Stage 1.3B — Standard cross-section geometry and deferred junction zones

Status: Implemented, locally validated, committed, pushed, and hosted-run confirmed at
`8bc8cf66723121cc764bcc48ee2623a023c3226c`. The user verified all four
backend/frontend Ubuntu 24.04/Windows 2025 jobs green. Exact duration and artifact
status were not supplied and remain unknown.

Stage 1.3B adds framework-independent exact nominal two-dimensional geometry for
standard W/wide-flange and general I sections, channels, tees, rectangular and square
tubes, angles, plates/doublers, and round tubes. The canonical section plane is local
`y-z`; local `x` is the future extrusion direction and remains deferred. Every
standard section uses the center of its nominal outside bounding box as construction
datum, distinct from centroid, shear center, analytical line, joint point, and force
reference point.

Flat-wall profiles use the approved nominal sharp-corner idealization. Physical
element geometry remains associated with the explicit Stage 1.2 topology identities.
Angle heels, tube corner squares, and web/stem-flange meeting boundaries are preserved
as stable non-targetable deferred features, while round tubes use exact analytic
annular geometry independent of any future display tessellation. The focused
Stage 1.3B section, import-boundary, and architecture suite passed 188 tests with 100%
line and branch coverage for the new section-geometry module. The complete backend
suite passed 497 tests with 100% line and branch coverage, and the unchanged frontend
retained 35 passing tests, 100% coverage, a successful build, and zero audit findings.

This stage does not add custom or solid-round geometry, section properties, member or
connector placement, extrusion, global transforms, faces, holes, bolt penetration,
interface targeting, material properties, structural credit, engineering equations,
resistance, capacity, API or frontend integration, persistence, visualization, or
reports. Geometry support remains distinct from calculation support.

### Stage 1.3C1 — Component placement, longitudinal extrusion, and physical end planes

Status: Implemented, committed, pushed, and hosted-run confirmed at
`080632786f963173f7c3ebeaf7981e9e09575709`. All four backend/frontend
Ubuntu 24.04/Windows 2025 jobs passed in 1 minute 25 seconds with no artifacts.

Stage 1.3C1 gives each placed member or connector exactly one proper resolved global
frame. Component local `x` is the longitudinal extrusion direction. A member frame
origin is physical START, local `+x` remains START-to-END regardless of connected end,
and its exact extent is `[0, member length]`; a connector uses its explicit resolved
frame and an explicit finite extent with `x_end > x_start`, including valid extents
that straddle zero. No placement uses reflection, scaling, shear, hidden mirroring, an
unresolved parent hierarchy, or a second roll/clocking state.

The component reference line remains the resolved frame's local `x` axis through
local `(y, z) = (0, 0)`. A separate explicit finite local `y-z` offset locates the
Stage 1.3B section construction datum and shifted section-datum line; no offset is
inferred from section family, symmetry, centroid, shear center, connector kind,
member role, or another component. Exact Stage 1.3B rectangles become local
rectangular-prism descriptors, annuli become exact finite annular-cylinder
descriptors, deferred rectangles and junction lines become non-targetable deferred
prisms or zero-thickness ruled surfaces, and nominal rectangular voids become exact
void prisms. Source cross-sections stay unchanged, separate physical-element identity
and shared material-region identity are preserved, and no Boolean solid or mesh is
created.

Every placed component exposes exact minimum- and maximum-local-`x` physical boundary
planes with outward global normals equal to `-local x` and `+local x`. Member START
maps to the minimum boundary and END to the maximum boundary; the connected-end plane
derives deterministically from `AssemblyMember.connected_end` without reversing the
member frame, extent, action sign, or START/END identity. Connector boundaries retain
neutral minimum/maximum identity and acquire no member-end semantics.

This stage is renderer-neutral component geometry only. It adds no `JointAssembly`
placement registry, analytical joint point, connection-face targeting, interface
placement, holes, bolts, force-reference placement, automatic moment shifting,
eccentricity discovery, section properties, engineering equation or result, API,
frontend, persistence, report, dependency, or lock change. Physical end planes and
deferred features remain non-targetable. Stage 1.3C2A subsequently adds canonical
surface patches without changing these placement contracts; interface targeting
belongs to Stage 1.3C2B. Hole/bolt placement and force-reference/eccentricity
integration belong specifically to Stage 1.3C3.

### Stage 1.3C2A — Physical surface patches and bounded support surfaces

Status: Complete, locally validated, and hosted-CI verified. Commit
`5e33db84e9efaf98fb5269b6df047be609c24506` passed all four backend/frontend
Ubuntu 24.04/Windows 2025 jobs in 1 minute 25 seconds with no artifacts.

Stage 1.3C2A derives stable, deterministic surface-patch identities from the exact
Stage 1.3B section geometry and Stage 1.3C1 placed extrusion. Surface patches remain
separate from physical section elements: one physical element may generate multiple
exterior, void-facing, and longitudinal end-cut patches while preserving the same
source element and material-region relationship. Every patch retains explicit source,
exposure, regular/deferred disposition, exact analytic geometry, and targetability
derived from those states rather than stored as an independent override.

Planar rectangles and annuli use proper surface frames whose local `+x` is the signed
geometric normal. Round-tube outer and inner surfaces remain analytic cylinders with
outward and inward radial sense, plus exact annular end cuts. The signed geometric
normal is not an FRP LW/CW/TT material axis. W/I, channel, and tee faces split around
deferred junction strips; rectangular-tube corners and the angle heel remain explicit,
bounded, and non-targetable. Physical-element minimum/maximum end cuts remain separate
source-owned patches rather than a merged component cap.

The stage also permits an explicit bounded planar rectangular support surface defined
by a proper frame and positive finite extents. It creates no inferred infinite plane,
support body, thickness, contact behavior, stiffness, bearing area, or structural
credit. Component surface references resolve only within the owning participant scope.

This stage adds no `ConnectionInterface` target field, connection zone, attachment,
load-transfer assignment, hole, bolt, force-reference placement, moment shift,
eccentricity integration, section property, engineering equation or result, API,
frontend, persistence, report, dependency, or lock change. Interface-to-surface
targeting is implemented separately by Stage 1.3C2B. Holes, bolts, force points, and
eccentricity belong to Stage 1.3C3.

### Stage 1.3C2B — Planar interface targeting, connection zones, and local frames

Status: Complete, locally validated, and hosted-CI verified. Commit
`2808348f44d308a4ddb30db0b2f89dc01e706da1` passed backend and frontend on
Ubuntu 24.04 and Windows 2025 in 1 minute 53 seconds with no artifacts.

This stage preserves `ConnectionInterface` unchanged and associates its exact
participant order with bounded whole-patch and rectangular-subzone surface selections.
It supports multi-patch planar sides, including the separate W/I web/flange and
rectangular-tube wall end patches, whole round-tube annular ends, direct member-to-member
interfaces, and bounded support surfaces. It adds one explicit primary zone, interface
origin, caller-supplied in-plane reference, proper interface-local frame, explicit
distance/angular comparison tolerance, and raw signed plane separation.

The stage is geometry only. Analytic cylindrical-side targeting, partial annular zones,
contact/fit, holes, bolts, force reference points, eccentricity, properties, equations,
capacities, results, API, frontend integration, persistence, and reports remain absent
from C2B.

### Stage 1.3C3 — Bolt paths, reference geometry, and explicit transforms

Status: Implemented, locally validated, committed at
6ce3f5f06f14d8e4f29410d7ef7ba9b61e557b75, and hosted-CI verified. All four
backend/frontend Ubuntu 24.04 and Windows 2025 jobs passed in 1 minute 21 seconds;
no artifacts were produced.

Stage 1.3C3 completes the planned Stage 1.3 renderer-neutral geometry foundation. It
adds explicit surface semantic roles, a staged complete joint geometry context, exact
symbolic-frame bindings, primary-interface bolt-group placement, authoritative master
centers and axes, analytic round-hole cylinders, intended physical-element layers,
ordered path-specific stacks, complete-disk containment with raw clearances, multi-
interface representation rules, physical reference-point resolution, manual-action
global resolution at an unchanged point, and caller-directed explicit eccentricity
transforms with full provenance.

The stage implements no bolt hardware, drilling approval, contact/fit, access,
interference, source mapping, load path, force distribution, combined demand,
equilibrium, capacity, utilization, result, API, frontend, persistence, or report.
Stage 2.1A later adds the bounded source/property/input/applicability contracts below;
the approved four-slice calculation sequence remains unchanged.

## Stage 2 — Calculation foundation

### Stage 2.1A — First-slice contracts and applicability

Status: Implemented, locally validated, and hosted-CI verified. Commit
`1804a6ff1a963966357b975c7d2d975a5e731a35` passed all four Ubuntu 24.04 and
Windows 2025 backend/frontend jobs in 1 minute 22 seconds with no artifacts.

Stage 2.1A adds one Decimal physical-quantity engine with U.S./SI profiles, published-
source and authoritative standard-hole records, calculation/material/fastener
snapshots, resolved demand, exact C3 geometry-to-code mapping, Chapter 8 prerequisite
validation, fail-closed applicability/readiness and aggregate statuses, canonical
SHA-256 fingerprints, the approved RC2 golden fixture, and an original non-executable
equation catalog.

No resistance, capacity, utilization, or calculated physical-input PASS/FAIL was
included in Stage 2.1A; those remain separated into the bounded Stage 2.1B slice.

### Stage 2.1B — Single-bolt numerical engine

Status: Implemented, locally validated, and hosted-CI verified. Commit
`4931c201222816119fc1871fb4dce70d2f5d7afe` passed all four Ubuntu 24.04 and
Windows 2025 backend/frontend jobs in 1 minute 48 seconds with no artifacts.

Stage 2.1B executes only authorized ready single-bolt plans for pure/combined metallic
bolt checks, pull-through, pin bearing, net-section tension, shear-out, and both
cleavage branches. It adds immutable equation traces and final results, exact
factor/property provenance, exact-boundary comparisons, fail-closed aggregation,
governing ties, complete RC2 golden reproduction, and exact converted U.S./SI
equivalence.

It does not add multirow or block-shear equations, demand distribution, generated
prying, automatic return-element credit, whole-J1 qualification, API, frontend,
persistence, reports, or commercial validation. The product remains unusable as a
general engineering design tool.

### Stage 2.2A — Canonical single-bolt orchestration

Status: Implemented, locally validated, and hosted-CI verified. Commit
`31b4785c88bb0b3564b7db9cc00b7206e3d2daa2` passed all four Ubuntu 24.04 and
Windows 2025 backend/frontend jobs in 1 minute 33 seconds with no artifacts.

Stage 2.2A adds one framework-independent application entry point that resolves an
exact canonical assembly/context, planar interface, logical bolt group and location,
authoritative bolt path, ordered penetrated layers, explicit material and fastener
assignments, load combination, and explicit resolved one-bolt demand. It reuses the
Stage 2.1A mapper/planner/fingerprint and Stage 2.1B numerical evaluator without
changing equations or approved golden values. An undistributed member-end action
fails closed; no equal sharing, group distribution, automatic moment shift, prying,
multirow equation, or block shear is introduced.

The frontend engineering workflow, project persistence, and reporting remain not
started. The calculation scope remains single bolt/single row.
Locked ASTM F593 `Fnt` source resolution remains pending, ICE properties remain
development-only and engineering-review-required, and the J1 angle-to-W connection
remains unresolved under the Section 2.3.2 qualification boundary.

### Stage 2.2B — Stateless single-bolt API exposure

Status: Implemented and locally validated in this task; commit/push evidence and the
pending hosted-CI disposition are recorded in the Stage 2.2B completion report.

Stage 2.2B adds one trusted-identity-bound, stateless
`POST /api/v1/calculations/single-bolt/evaluate` route. Strict versioned DTOs carry
decimal strings and explicit units, production mapping rebuilds canonical domain and
geometry objects, and the route calls the Stage 2.2A service exactly once. A valid
engineering fail, unsupported, source-pending, review-required, qualification-
required, or no-distribution outcome remains HTTP 200; malformed transport or
canonical mapping fails with HTTP 422.

No equation, golden value, orchestration behavior, frontend workflow, persistence,
report, multirow equation, block shear, automatic demand distribution, or generated
prying is added. Trusted identity, engine/rule versions, fingerprints, statuses, and
calculation results are server-authoritative.

## Foundation geometry prototype

Create a non-calculating canonical assembly prototype containing one supporting column, one beam, two braces, one shared T connector, one direct bolted interface, one doubler, multiple member directions, bolt groups, and ordered bolt stacks. Provide synchronized 3D, Front, Side, Top, and at least one interface-normal or section view, including positive-sign and applied-force arrows.

The prototype demonstrates topology, selection, canonical geometry, views, dimensions, and unsupported-state handling. It must produce no engineering `PASS` result and must not imply calculation coverage.

## Approved target calculation slices

Each slice requires source mapping, an explicit applicability definition, independent verification, and qualified engineering approval before implementation or commercial validation.

### First target slice

Direct angle brace to W-section column flange.

Key preparation includes resolving the engineering basis for the unsymmetrical angle-brace connection, sign/reference-point rules, bolt group and stack modeling, material qualification, unsupported-condition triggers, and joint-level result aggregation.

### Second target slice

Angle-column leg directly connected to the back of a channel beam.

Work reuses only verified common concepts; applicability and load path remain specific to this topology and must be approved independently.

### Third target slice

One T-section connecting one beam and two braces to a column.

Work must address combined T demand, T-to-support demand, shared bolt-group demand, shared supporting-region demand, T material options, and qualification basis. Independent interface checks alone are insufficient.

### Fourth target slice

Moment beam-to-column connection with optional braces.

Work begins only after an approved engineering basis exists for moment-resisting FRP joints. Moment and shear effects that share components or FRP regions must be treated together as required by the approved method.

## Reporting and formal validation

- Define Summary and Detailed report presentations over the same immutable calculation snapshot.
- Complete source, applicability, unit, hand-calculation, known-outcome, boundary, invalid-geometry, force-direction, combined-action, persistence, fingerprint, visual, report, security, and project-isolation verification gates.
- Obtain qualified engineering review for every commercial calculation family.
- Freeze approved artifacts, versions, rule sets, source/errata sets, evidence, and hashes for a release candidate.
- Do not represent a family as commercially validated until its engineering review gate is approved.

## SaaS deployment

- Select authentication, account, entitlement, billing, hosting, database, object storage, email, and deployment providers through controlled decisions.
- Integrate with the MES website and Client Login while preserving separable public-site, portal, API, calculation, persistence, storage, and audit boundaries.
- Enforce identity, ownership, organization membership, entitlement, project isolation, and report access on the server.
- Preserve calculation-core independence from authentication, payment, billing, email, and website providers.

## Future FRP Master interchange

Define a controlled, versioned interchange method only after the product’s canonical contracts and ownership boundaries are stable. FRP Master Connection remains a separate repository from FRP Master Pro; interchange must not become source copying, repository coupling, or an implicit force-import dependency.

## Optimization

Automatic connection selection and optimization may begin only after the checking engine and relevant calculation families are validated. Optimization may select only among approved capabilities and may never conceal an unsupported, incomplete, stale, or engineering-review-required condition.

Stage 1.3C3 completes the implemented geometry foundation. Stage 2.1A provides the
contract/applicability boundary and Stage 2.1B provides only the authorized narrow
single-bolt numerical slice. Stage 2.2A provides its canonical in-memory orchestration
boundary, and Stage 2.2B provides its stateless transport. The frontend engineering
workflow begins in Stage 2.3 only for the verified session-only single-bolt/single-row
Shear slice. It adds canonical visualization, J1 U.S./SI input loaders, supported
inputs and demand modes, and server-authoritative results without broadening the
calculation method. Moment remains not implemented.

## Stage 2.3R — combined workspace visual/workflow refinement

Stage 2.3R refines the same Stage 2.3 workspace as one combined implementation. It
uses a proper rigidly rotated J1 visual fixture with a vertical W column and diagonal
brace while preserving the approved 45-degree relationship and verified results. It
adds solid role-based presentation, a connection-first sidebar/viewer/results layout,
clean default overlays, improved camera fit, compact presentation-only number
formatting, friendly labels with stable IDs, clearer demand provenance, and integrated
inspectors. It adds no calculation method, transport behavior, persistence, report,
or dependency.

## Stage 2.3R2 — controlled geometry inputs and portability correction

Stage 2.3R2 keeps the verified calculation scope unchanged while moving the narrow
brace-to-column-flange template construction behind the backend boundary. It adds an
editable acute brace angle, longer local W-column extents above/below a fixed
connection station, editable brace segment length, exact-known shank/washer display,
full-extent connection-centered camera fitting, and the refined W-flange/material/
demand labels. The current slice remains in a vertical plane with plan angle fixed at
0 degrees.

It also reconciles the Stage 2.3R Ubuntu-only exact-float test failure using the
existing mathematical tolerance without production clamping. The 45-degree J1
engineering results, qualification, governing check, ICE review requirement, F593
source-pending boundary, Stage 2.1B equations/goldens, orchestration meaning, and API
engineering statuses remain unchanged.

Hosted Stage 2.3R2 commit `a28f83c2f124c3dcd994de250a1de962ef8ff72f`
failed only Backend / Ubuntu 24.04: 1,068 of 1,069 backend tests passed, and the one
failure compared exact serialized strings for a finite derived bolt-center point.
Backend / Windows 2025 and both frontend jobs passed in the 1 minute 58 second run;
no artifacts were shown. The narrow correction uses a test-only, scale-relative
machine-epsilon comparator for derived physical positions while preserving the
existing dimensionless vector tolerance. It changes no production geometry,
serializer, equation, golden, fingerprint policy, qualification, extent, or UI. The
correction commit `c2114fbb2b4c3c99568669407b97aa1b944cd75b` was subsequently
verified by the user with all four hosted jobs green in 1 minute 54 seconds and no
artifacts.

## Stage 2.3R3 — connection orientation and axis refinement

Stage 2.3R3 remains within the same verified single-bolt workspace. It adds semantic
exterior/web-side W-flange contact, connected angle Leg 1/Leg 2, discrete
outstanding-leg orientation, backend section clocking, exact contact/path mapping,
and bounded fail-closed angle/W interference. It also adds selected contact-face
highlighting, synchronized model/sidebar selection, a persistent corner global triad,
separate optional full model axes, selected-member local axes, material labels, and an
axis/action legend. It changes no equation, golden, demand distribution, orchestration
meaning, persistence, or report boundary.

## Stage 2.3R4 — viewport-navigation correction

Stage 2.3R4 corrects only the interactive viewer. One OrbitControls lifecycle provides
left-drag orbit, right-drag pan, and wheel zoom; direct SVG corner-triad updates avoid
per-camera-change React workspace state; pointer gesture isolation preserves both drag
navigation and canonical click selection. Static overlays are pointer-transparent,
cleanup is explicit, and repeated view/Fit/Reset operations cannot submit an API
request or stale a result.

Navigation is presentation-only. Members remain non-draggable, and their physical
geometry remains controlled by existing engineering inputs. No equation, golden,
orientation, material mapping, qualification, fingerprint, unsupported engineering
method, persistence, or report behavior changes.

## Recommended work after Stage 2.3R4

The next activity is user visual/navigation review of the pushed R4 workspace and its
hosted four-job CI result. No next engineering stage, bolt-demand distribution,
multirow method, block shear, persistence, or reporting work is authorized by this
roadmap update.

### Deferred capabilities remain unauthorized

The following is planning only and is not started:

1. Complete user visual verification of the canonical workspace.
2. Approve and independently verify a bolt-demand distribution methodology.
3. Add separately controlled multi-bolt/multirow equations and benchmarks.
4. Define qualified custom manufacturer material editing and library controls.
5. Implement persistence, authentication, account, ownership, and entitlement only
   after their security and migration contracts are approved.
6. Define Summary and Detailed reports over immutable calculation snapshots.

Stage 2.3R4 does not authorize any item in this list. User visual/navigation acceptance and
hosted-CI review remain the next activities before any new engineering-method work.

## Stage 2.3R5 — live canonical preview and explicit design check

Stage 2.3R5 keeps the current verified single-bolt engineering scope and makes the
backend canonical model responsive to controlled sidebar edits without automatically
running design resistance. A stateless preview service returns geometry, action,
material relationship, contact/path, interference, model status, and design readiness.
`Run Design Check` remains the only route to capacity, utilization, governing status,
qualification, and fingerprint results.

The workspace separates model/geometry status from design status, preserves but marks
old design results stale, blocks design for invalid or incomplete geometry, and uses
debounce, cancellation, and latest-response-wins controls. Stage 2.3R5 adds no new
engineering method, persistence, or report capability.

## Recommended work after Stage 2.3R5

User UI acceptance of live preview, explicit design-check separation, and the pushed
four-job hosted CI result remains required before any next engineering-method stage.
Bolt-demand distribution, multirow methods, block shear, persistence, and reporting
remain unauthorized and have not begun.

## Stage 2.3R6 — corrected geometry/view model and interactive loads

Stage 2.3R6 separates authoritative connection ends and `e1` from preview-only local
member extents, supports directed vertical-plane brace angles throughout `(0, 180)`,
and adds signed applied-action labels/editors over the existing single load state.
It also corrects the R5 hosted frontend timeout by reducing redundant test work. The
calculation engine, rule set, RC2 goldens, explicit-design boundary, and supported
engineering methods remain unchanged.

## Recommended work after Stage 2.3R6

User interface acceptance and direct verification of the four-job hosted R6 result
remain required before any next engineering-method stage. Bolt-demand distribution,
multirow methods, block shear, persistence, reporting, and other new engineering
methods remain unauthorized and have not begun.

## Stage 2.3R7 — projected action values and shared editing

Stage 2.3R7 completes the approved action-value presentation refinement within the
same verified workspace. Every visible applied force and moment value is projected
beside its own arrow or arc and remains camera-associated through navigation and view
commands. Labels preserve source/component/sign/unit, use the existing shared action
state and editor, and follow applied/value/zero visibility. Positive-convention labels
remain separate and noneditable. No backend engineering, equation, golden, API,
dependency, persistence, report, or unsupported-method scope changes.

The prior R6 hosted result is reconciled as four of four Ubuntu/Windows backend and
frontend jobs successful in 2 minutes 31 seconds with no artifacts. R7 complete local
and automated browser gates pass; only manual right-button pan and direct hosted R7
verification remain. Bolt-demand
distribution, multirow equations, block shear, persistence, reporting, and another
engineering stage remain unauthorized and have not begun.

## Stage 2.3R8 and Stage 2.3F - interface/geometry foundation freeze

Stage 2.3R8 completes the approved applied-action label styling and moment-arc
attachment refinement. Its exact implementation commit
`5bc545ab8251f9bd49dedc776962937ed5e822a2` passed all four hosted backend/frontend
Ubuntu 24.04 and Windows 2025 jobs in 2 minutes 23 seconds, with 123 frontend tests on
each platform and no artifacts. Baraa Misto approved the resulting Stage 2.3
interface and geometry foundation for a scope-specific freeze. Annotated tag
`stage-2.3-interface-geometry-freeze` permanently identifies that implementation
baseline.

The Stage 2.3 workspace, canonical geometry/visualization authority, live-preview
versus explicit-design workflow, current geometry controls, axes/actions, viewport
navigation, and applied-action interaction contracts are complete and frozen. The
overall product remains draft and under development; this is not a production
release. Next controlled work returns to engineering-method derivation and
verification. A future change to any frozen behavior requires the recorded controlled
freeze-change review, regression against the R8 tag, and Baraa Misto approval when
engineering meaning or the approved workflow changes. Compatible extensions that
preserve the frozen invariants may proceed under normal controlled development.

## Stage 2.4A - general multi-row contracts and calculation planning

Stage 2.4A is implemented locally as a compatible backend-only extension to the
frozen Stage 2.3 foundation. It establishes unrestricted physical bolt-group
geometry; force-relative row/line resolution; prescribed, conservative full-row, and
engineer-defined demand plans; first-row and inter-row method plans; physical L/U
block paths; half-hole accounting; raw net-area status; explicit source conflict;
applicability/qualification hierarchy; deterministic fingerprints; and controlled
Slice 2 RC1 fixtures.

This stage deliberately executes no new resistance, capacity, utilization, or
calculated physical-input PASS/FAIL equation. The next recommended controlled stage
is Stage 2.4B numerical implementation and independent verification of the approved
plans. It must receive its own controlling order and may not alter the frozen Stage
2.3 behavior or approved Slice 1 RC2 results.

Stage 2.4A feature commit `b7411ef8da08bb7134bd7f83e729cfc3a90f77d8`
subsequently failed both hosted backend jobs in one checkout-dependent raw-byte
freeze audit; 1,239 other backend tests passed at 100% coverage, both frontend jobs
passed 123 tests, the four-job run ended after 2 minutes 33 seconds, and no artifacts
were shown. Stage 2.4A-R1 keeps the freeze strict by comparing the frozen
`frontend/src` Git tree and two package-file blobs, and corrects the inaccurate
completion/documentation description of the already-correct four prescribed
material-pair distributions. Full R1 local validation passed 1,254 backend and 123
frontend tests at configured 100 percent coverage, the complete integrated gate, and
both zero-vulnerability audits. Hosted correction evidence is still required before
Stage 2.4A is accepted.

## Stage 2.4B - Calculation Slice 2 numerical implementation

Stage 2.4B implements the owner-approved RC2 numerical authority as a compatible,
backend calculation-layer extension. One pure entry point consumes a deeply immutable
execution bundle and returns deterministic per-check plus aggregate results. It
executes the approved per-bolt reuse, simplified/full/lower-envelope first-row,
inter-row, pitch-factor, block-shear, eccentricity, multi-layer, and fail-closed status
behavior. RC1 remains the planning/regression authority and RC2 controls numerical
meaning, golden results, and trace expectations.

The Slice 2 contract/engine/rule identities are `2.4B-RC2`, `0.2.0.dev1`, and
`asce74-23-ch8-multirow-rc2.dev1`; execution-input, result, and fingerprint schemas are
separately versioned `0.1.0-draft`. The existing Slice 1 engine/rule set and golden
remain unchanged. Source-derived methods and explicitly identified rational/project
methods remain distinguishable in every result, and applicability/qualification gates
prevent an ordinary ASCE PASS outside the prescribed scope.

This stage adds no application orchestration, API, frontend, persistence, reporting,
authentication, billing, deployment, general bolt-group equilibrium, friction/slip,
or automatic prying. The Stage 2.3 scope freeze and its tag remain unchanged.
Stage 2.4B implementation commit pushed; local verification passed; hosted CI pending
user verification.

## Stage 2.4C-R3 - unified integration and solid fastener hardware

Stage 2.4C implements the first usable multi-row vertical slice without changing the
accepted Stage 2.4B equation meaning. Framework-independent orchestration constructs
canonical geometry, plans, materials, factors, required checks, and visualization.
Preview executes zero equations; explicit design calls the verified engine once.
Exactly two stateless API operations expose those services through strict transport
contracts.

The existing Shear Connections workspace remains the sole connection workflow. Single
bolt / single row remains the default; row and bolt count are edited in that same
sidebar. Supported multi-row arrangements retain the same canonical Three.js scene,
camera, overlays, picking, actions, inspectors, and result region while using the
accepted multi-row preview/design path. The backend supplies all physical bolt/hole/
washer coordinates, penetrated layers, and the external connection resultant. Live
preview, explicit Run Design Check, stale/out-of-order protection, public engineering
controls, and deterministic result traces remain.

Stage 2.4C does not add automatic member-end or bolt-group demand distribution,
friction credit, generated prying, persistence, reports, authentication, billing,
deployment, or another engineering method. The Stage 2.3 tag remains unchanged; its
historical frontend tree and the hosted-green but visually rejected separate-workspace
tree remain provenance. The unified R1 source tree is the accepted historical
predecessor and its exact commit passed all four hosted jobs in 2 minutes 54 seconds,
with 167 frontend tests and no unexpected artifacts.

R2 keeps the unified architecture and independently renders canonical bolt and
physical-hole diameters with exact-radius visibility outlines. The multi-row physical
scene uses the same backend-derived standard hole as the engineering snapshot. The
former `+X` orthographic Side preset becomes Side 1 and exact opposing `-X` becomes
Side 2; both retain the same target and `+Z` up direction. Picking, Fit/Reset, preview,
stale/design separation, equations, results, fingerprints, dependencies, and the
original freeze tag remain unchanged. The staged active `frontend/src` successor is
`f95396ff7ce5604f08f40b04b7b5e216271672c6`. R2 hosted CI and user post-push
visual acceptance are pending and are required before formal Stage 2.4C acceptance.

R3 preserves the R2 canonical diameter and five-view camera behavior while replacing
the bolt-shank cage overlay with a capped opaque shank and one common fastener assembly
renderer. Authoritative washers remain exact. Schematic frontend-only hex heads and
nuts use the controller-approved `1.50d` across-flats, `0.625d` head-height, and
`0.875d` nut-thickness presentation ratios pending an authoritative dimensional
hardware model. They do not enter backend geometry, APIs, fingerprints, interference,
calculations, or reports. The staged active source successor is
`deb93d01646580ae62c9f90a507446d84019bd87`; R2 remains historical provenance.

## Stage 2.5A - eccentric bolt-group demand analysis

Stage 2.5A implements the owner-approved Calculation Slice 3 RC1 as a compatible,
framework-independent calculation-layer extension. It consumes canonical global force
and force-reference data, an exact interface frame, the physical rectangular bolt
group, and each existing accepted Stage 2.4A direct-demand scenario. It calculates the
geometric centroid and polar coordinate sum, the force-line and direct-distribution
moments, the residual equal-stiffness elastic correction, per-bolt total vectors, and
explicit force/moment equilibrium residuals.

The stage preserves every ASCE-prescribed, conservative full-row, and engineer-defined
scenario independently with method/provenance and applicability/qualification. Pure
moment and eccentric degenerate groups fail closed; nonzero member-end moments and
interface-normal force remain visible warnings without being transferred into bolt
tension or prying. Automatic resistance handoff is prohibited in RC1.

This stage adds no API route/schema, application orchestration, frontend behavior,
dependency, persistence, report, authentication, billing, deployment, or Stage 2.3
freeze change. A future controlled compatibility review is required before eccentric
per-bolt vectors may enter directional FRP resistance checks. Stage 2.5A does not begin
that integration.

## Stage 2.5B - eccentric demand-to-resistance compatibility handoff

Stage 2.5B implements the owner-approved resistance-handoff RC1 as a backend
calculation-layer extension. It preserves the accepted Stage 2.5A result and its
fingerprints, carries each actual total bolt vector into supported per-bolt checks,
resolves per-layer LW/CW bearing selection at the approved 5-degree boundary, and
delegates numerical resistance only to the unchanged Stage 2.4B executor.

Full legacy collinear cases reproduce the accepted Slice 2 outcome without numerical
change. Nonzero residual moment authorizes bolt shear, explicit-axis combined checks,
explicit-axis pull-through, and per-bolt/per-layer bearing diagnostics. Eccentric
first-row net tension and inter-row shear-out remain required but unsupported. Existing
block shear is conditional on an exact canonical force-line/path identity match.
Incomplete action transfer prevents whole-connection PASS, and supported numerical
FAIL retains precedence.

The stage adds immutable contracts, deterministic fingerprints, controlled fixtures,
and calculation tests only. API, application orchestration, frontend behavior,
dependencies, persistence, reports, authentication, billing, deployment, new
resistance equations, and later engineering stages remain out of scope.

## Stage 2.5C - automatic member-end force integration

Stage 2.5C is the first end-to-end use of the accepted force-only eccentric demand and
resistance-handoff engines. The unified workspace defaults to explicit resolved demand
for backward compatibility and can select automatic member-end force. Preview calls
Stage 2.5A only and returns backend-authored bolt vectors with zero resistance work.
Explicit **Run Design Check** calls Stage 2.5A and then Stage 2.5B; application and API
layers never bypass Stage 2.5B to call eccentric resistance directly.

This stage integrates existing methods and adds no equation. Nonzero member-end moments
remain untransferred, out-of-plane force does not generate axis demand, eccentric
first-row net tension and inter-row shear-out remain unsupported, and block shear
remains compatibility-gated. General six-component distribution, new resistance,
persistence, reporting, authentication, billing, and the next engineering stage remain
future controlled work.

The transport/application identities advance to `2.5C-RC1` and `0.2.0-draft` without
changing Stage 2.4B, 2.5A, or 2.5B numerical identities. The active controlled
`frontend/src` successor is `035af7c9c44c49914c63c68edf6ec4eea5521bcf`; all earlier
accepted trees remain historical provenance and the original Stage 2.3 tag remains
immutable.

### Stage 2.5C-R2 - loaded-boundary bolt-group placement correction

R2 fixes the first broken propagation point in multi-row application geometry. The
loaded-boundary field previously enlarged a planar boundary but did not move any row
or bolt, and the physical snapshot independently pinned Row 1 to the selected
single-bolt display. The loaded boundary is now fixed in the existing local placement
coordinate system; Row 1 and the complete group translate from the independent
loaded-boundary distance while pitch, gauge, hardware dimensions, identities, and
connected-member display geometry remain unchanged.

The corrected canonical coordinates flow through block paths, fingerprints, the
optional 2D diagnostic, the one shared five-view scene, and the accepted Stage 2.5A
centroid/eccentricity input. Preview remains equation-free and design remains explicit.
No calculation equation, controlled specification, golden, transport schema,
dependency, persistence, report, or freeze tag changes. Active frontend source tree
`cd25332af05c101aa008934a7b900d8c220938f3` succeeds the R1 tree; hosted CI and direct
user visual acceptance remain pending.

## Stage 2.6A - eccentric group-mode compatibility

Stage 2.6A is a framework-independent backend calculation extension over the accepted
Stage 2.5A demand and Stage 2.5B handoff results. It uses canonical physical bolt-line
membership, sums actual bolt vectors, and authorizes inter-row shear-out only when the
exact line resultant is zero or positive-parallel to the connection-force direction.
Nonparallel and reversed lines remain explicit unsupported states.

Authorized lines reuse the existing Stage 2.4B Equation 8-12, Equation 8-13, and
qualified more-than-three-row paths under the named rational actual-line-resultant
handoff. Zero residual moment retains legacy behavior. General eccentric first-row net
tension remains unsupported, exemptions remain explicit, and known supported failures
retain precedence. No new resistance equation, frontend, API, application integration,
dependency, persistence, report, or later engineering-stage behavior is introduced.

## Stage 2.6B - eccentric group-mode end-to-end integration

Stage 2.6B connects the accepted Stage 2.6A result to the existing Stage 2.5C automatic
design workflow without changing any calculation engine. Automatic preview remains a
Stage 2.5A-only, resistance-free operation. Explicit **Run Design Check** builds the
existing Stage 2.5B handoff, invokes Stage 2.6A once per scenario with the exact parent
objects, and merges the returned statuses without recalculating a bolt-line resultant.

The stateless API transports exact line vectors, authorized scalar demand, resistance,
utilization, warnings, trace, and Stage 2.6A identities. The unified workspace displays
those backend results separately from the still-unsupported eccentric first-row
net-tension mode. Supported FAIL has precedence; supported PASS plus a required
unsupported first row remains not evaluated. Zero-residual legacy and explicit-demand
workflows retain their accepted presentation and result meaning.

This integration adds no equation, demand-distribution method, golden, persistence,
report, dependency, or workflow file. Active frontend source tree
`b73469788561bebf61262f2211b6e39c3e3a699c` is the controlled successor; earlier
trees remain historical and the original Stage 2.3 tag/target remains immutable.

## Stage 3 - connection-platform and connection-library program

Stage 3 generalizes the accepted connection foundation into reusable physical
assemblies, explicit connector and fastener materials, controlled property provenance,
and later connection libraries. The programs below are roadmap groupings, not a claim
that every later stage number, engineering authority, geometry family, or calculation
method has been approved or implemented.

### Stage 3.1 - connector and fastener material architecture

Stage 3.1 establishes immutable backend-only architecture for connector material
families `PULTRUDED_FRP`, `STAINLESS_STEEL_316`, and `CARBON_STEEL`; fastener
families `STAINLESS_STEEL_316`, `CARBON_STEEL`, and `CUSTOM_FRP`; and behavior
families `DIRECTIONAL_FRP`, `ISOTROPIC_METAL`, and
`CUSTOM_FASTENER_MATERIAL`. Topology and material are independent, and engineering
property values remain tied to explicit controlled provenance. Neither stainless nor
carbon-steel identity infers a grade or strength. A custom FRP fastener cannot enter
the existing metallic-bolt equations under any authority. A future custom-FRP
resistance path requires its own separately approved method and authority.

The strict compatibility boundary maps FRP/FRP to the existing `FRP_FRP` meaning and
FRP/316-stainless or FRP/carbon-steel to the existing `FRP_STEEL` meaning while
retaining exact metal subtype. Unsupported pairs reject without fallback. No Stage 2
engine, rule, result, fingerprint, golden, API/application behavior, frontend source,
dependency, workflow, or frozen Stage 2.3 identity changes.

Future mixed-material provenance may record exposure environment,
coating/protection, galvanic or isolation detail, and corrosion-review status. That
metadata is deferred; Stage 3.1 establishes no structural resistance, corrosion design
rule, durability credit, or automatic material-compatibility conclusion.

### Engineering coverage classes

The connection platform uses four high-level coverage classes without replacing
detailed applicability and qualification contracts:

- `PRESCRIPTIVE` (P): directly supported by the controlling standard with every
  prerequisite satisfied.
- `QUALIFICATION_REQUIRED` (Q): calculation mechanics may exist, but Section 2.3.2,
  test qualification, or another explicit qualification condition remains.
- `CROSS_CODE` (X): some required connection behavior is governed by another
  standard or authority, such as future concrete anchorage or metallic connector
  design.
- `RESEARCH_OR_PROPRIETARY` (R): no adequate accepted automatic method exists;
  research, test/manufacturer data, proprietary authority, or a separately approved
  rational method is required.

These classes organize product coverage only. They do not convert unsupported,
incomplete, unqualified, or source-pending work into a calculation success.

### Program A - connection-platform generalization

Develop reusable connection assemblies, connector components, interfaces, physical
support surfaces, one or more bolt/anchor groups, penetrated material stacks, local
frames, load-path relationships, and applicable-limit-state resolution.

### Program B - brace connection library

Develop controlled future templates for braces to column flange, physically applicable
column web, beam flange, and physically applicable beam web using FRP Tees, FRP
angle/clip-angle-type assemblies, qualified metallic Tee/angle variants, and later
defined gusset or shear-plate families.

### Program C - beam simple-connection library

Develop future beam-to-column-flange, beam-to-column-web, and beam-to-beam/girder
templates using clip-angle-type assemblies, Tee connectors, qualified shear plates or
gusset-like plates, and later supported seats or brackets.

### Program D - connector-component mechanics

Qualify future FRP connector failures, clip-angle/instep behavior, metallic connector
checks, plate/component local checks, and the associated qualification limitations.
Stage 3.1 implements none of these resistance methods.

### Program E - truss, gusset, and multi-member nodes

Develop future chord-diagonal nodes, verticals, gusset plates, multi-interface panel
points, and chord/member splices with explicit assembly and load-path authority.

### Program F - member splices

Develop future tension/brace, beam, column, compression-bearing, and flange/web splice
systems under separately approved geometry and engineering methods.

### Program G - bases, supports, foundations, and anchorage

Develop future FRP-column-to-concrete connections using base angles, base plates,
Tee/bracket bases, anchor rods/groups, and top- or side-mounted support details.
Concrete bearing and anchorage are cross-code scope and require separate controlling
authorities; material identity alone provides no concrete or metallic resistance.

### Program H - handrail, guardrail, and access-system connections

Develop future post bases, rail-to-post and midrail details, rail splices, corner/T
joints, top- and side-mounted supports, and related platform/stair/ladder connections.

### Program I - platform, stair, ladder, and industrial FRP connections

Develop future reusable templates for stringers, landings, grating/support clips,
ladder brackets, and platform-framing details.

### Program J - advanced simple-connection mechanics

Reserve controlled research and qualification work for general eccentric first-row
net tension, nonparallel shear-out interaction, reversed-line failure paths, prying,
justified slip/friction, and connection stiffness/serviceability.

### Program K - moment and semi-rigid connections

Treat moment/semi-rigid connections as a separate major engineering program, not an
extension of the current simple/shear mechanics. Future topics include moment transfer,
moment-rotation behavior, stiffness, component deformation, prying, qualification/test
bases, and portal/knee, beam-column, beam-splice, and moment-base connections.

### Program L - productization

Reserve material/fastener catalogs, manufacturer libraries, project persistence,
reports, drawings, optimization, batch cases, and revision control for separately
controlled future development. Stage 3.1 implements none of these product services.

The repository term for roadmap purposes is `clip-angle-type`. The owner-provided
phrase “slip angle” remains terminology requiring confirmation only if it is later
intended to identify a distinct topology.

## Stage 3.2 reusable Tee connector vertical slice

Stage 3.2 is the first end-to-end Program A/B connection-family slice. It reuses one
backend-authoritative Tee topology for a brace connected through the Tee stem to either
a column flange or beam flange, with either physical support flange selectable. The
same component contract resolves the perpendicular flange/stem solids, selected
contact faces, member placement, and two physical bolt-group axes; the frontend does
not reproduce that placement logic.

Interface A (brace to Tee stem) and Interface B (Tee flange to support) retain
independent layouts, bolt groups, paths, layer stacks, demands, results, and
fingerprints. One global action/reference is resolved into both interface-local frames.
Authorized in-plane demand delegates to unchanged Stage 2.5A, Stage 2.5B, and Stage
2.6A paths. Nonzero interface-normal force remains visible and fail-closed: the slice
does not invent bolt-axis tension or prying. Tee-body resistance remains separately
`NOT_EVALUATED`, so supported interface PASS results do not become an ordinary
whole-assembly PASS; a known supported failure still governs FAIL.

The existing unified session-only workspace gains one Tee template, automatic
geometry/action preview, explicit **Run Design Check**, both physical bolt groups,
interface-grouped results, and engineering-change staleness. No persistence, report,
dependency, CI-workflow, new resistance equation, or Stage 3.3 work is included.
Implementation and local automated evidence are tracked under Stage 3.2. Stage 3.2-R1
passed all four hosted jobs with 227 frontend tests on each platform; owner V1 review
required the R2 workspace/profile correction and renewed visual acceptance.

## Stage 3.2-R2 unified workspace and reusable member profiles

Stage 3.2-R2 corrects the first visual-review findings without broadening the accepted
engineering slice. Direct and Tee brace connections share one connection-first shell,
one grouped selector, one persistent desktop viewer region, one independently
scrollable engineering sidebar, and one accessible result region. Physical interface
names replace the internal A/B language in the workspace while stable internal IDs are
retained for deterministic application and test linkage.

The connection platform now separates member role from profile family, exact profile
dimensions, material assignment, orientation, selected physical surface, and geometry
identity. Angle, channel, wide-flange/I, rectangular hollow, and flat-plate profiles
are production geometry for the current Tee slice. Round hollow is retained as a
future-capable identity but is not an applicable direct flat Tee-stem contact. Column
and beam roles reuse the same W-section profile geometry. Future Programs A through L
shall reuse this architecture instead of introducing template-specific duplicate
member or section models.

This correction does not add a profile catalog, default strength, member-body check,
Tee-body check, resistance equation, demand method, connection family, persistence,
reporting, or dependency. Interface A targets the selected real brace surface;
Interface B remains independent. Stage 3.2 stays open until hosted CI and V1-R2 through
V5-R2 visual acceptance are directly evidenced. Stage 3.3 has not begun.

## Stage 3.2-R4 exact-unit geometry and benchmark correction

R4 closes the two blockers documented by the stopped R3 diagnosis: authoritative SI
end distances are now calculated in exact Decimal space before validation/fingerprint,
and the U.S./SI Tee loader uses one physically valid flat-plate benchmark rather than
an Angle whose bolt paths missed the finite opposing face. Manual member-profile
selection and every R2 engineering boundary remain intact.

R4 adds no connection family, profile family, equation, capacity, demand method,
Tee/member-body method, persistence, reporting, dependency, workflow, or tag. Stage 3.2
remains open until four hosted jobs and V1-R2 through V6-R4 visual acceptance are
directly evidenced. Stage 3.3 has not begun.

## Stage 3.2-R5 cross-platform preview fingerprint correction

R5 repairs the cross-platform determinism defect exposed by the R4 hosted Backend
Ubuntu job. The semantic 45-degree brace angle entered canonical preview identity
through platform-libm sine/cosine results; a one-ULP sine difference expanded across
normalized physical geometry and changed both U.S. and SI preview hashes even though
the engineering case was unchanged.

The application now evaluates semantic degree angles deterministically before the
existing geometry path and verifies byte-identical canonical pre-hash payloads under a
simulated Ubuntu result. R5 changes no accepted fingerprint, geometry meaning, demand,
resistance, profile, connection family, frontend behavior, dependency, workflow,
report, persistence, or tag. Direct user evidence accepts all four hosted R5
Ubuntu/Windows backend/frontend jobs. Stage 3.2 remains open until the existing
V1-R2 through V6-R4 visual acceptance is directly recorded. Stage 3.3 has not begun.

## Stage 3.2-R6 live-preview state correction

R6 corrects the Tee workspace display contract without changing engineering meaning.
The investigated screenshot configuration is invalid because a 3-by-3 Interface A
group does not fit the entered Tee stem depth; valid profile and A/B layout edits were
already bound to the latest accepted response. The correction distinguishes current,
pending, invalid-with-last-valid, failed-with-last-valid, and no-valid-preview states,
shows backend validation detail, blocks design for noncurrent geometry, and clears
disappearing selections.

The active frontend tree is `40956420c22c42910a8a2cbe82c74ed2e6a5f407`.
Equations, geometry rules, controlled R4 artifacts/fingerprints, dependencies,
workflows, and the immutable Stage 2.3 freeze remain exact. Stage 3.2 stays open for
R6 hosted CI and renewed V1-R2 through V6-R4 visual acceptance. Stage 3.3 has not begun.

## Stage 3.2-R7 exact Angle bolt-path correction

R7 corrects the derived through-thickness path for a selected connected Angle leg. A
valid path now resolves exactly one finite exposed opposing broad boundary of the same
physical leg and retains the exact profile thickness. The existing solid heel remains
one non-targetable overlap region: a heel-strip path, free-edge exit, outside-leg
point, wrong patch, ambiguous candidate, or thickness mismatch remains fail-closed.

The backend-confirmed visual fixture uses a `6 x 6 x 0.5 in` Angle, `8 in` member
length, `LEG_Y_OUTER`, zero-degree orientation, a 2-by-2 Interface A group, `2 in`
pitch/gauge, and the smallest valid unloaded-end distance `1.2815 in`. The original
`1 in` case and `1.2814 in` remain invalid under complete-hole containment. R7 adds no
resistance, demand, material, applicability, profile, tolerance, fingerprint,
frontend production, dependency, workflow, persistence, report, or Stage 3.3 work.
R6 hosted CI is accepted four of four green; R7 hosted CI and renewed V1 visual
acceptance remain separate post-push evidence.

## Stage 3.2-R8 exact profile-wall bolt-path correction

R8 generalizes the exact owner-derived through-thickness path rule from Angle legs to
Channel web/flanges, W/I web/flanges, and all four RHS walls. The selected outer wall
must contain the complete hole footprint and map to exactly one finite exposed
opposing broad boundary of the same physical wall at the exact profile thickness.
Web/flange junctions, flange/web overlap, RHS corners, outside-wall points, wrong
patches, ambiguous candidates, and thickness mismatches remain fail-closed.

The controlled Channel and RHS fixtures use an `8 in` profile length, 2-by-2
Interface A, `2 in` pitch/gauge, and the smallest valid unloaded-end distance
`1.7815 in`; `1.7814 in` and the original `1.3 in` remain invalid under complete-hole
containment. RHS results explicitly carry `INTERNAL_FASTENER_ACCESS_REQUIRED`; this is
qualification only and does not authorize through-bolts, spacers, inserts, blind
fasteners, access feasibility, or RHS local-wall resistance. R8 changes no resistance,
demand, material/profile applicability, accepted fingerprint, frontend production
source, dependency, workflow, persistence, report, freeze tag, or Stage 3.3 scope.
R7 hosted CI is accepted four of four green; R8 hosted CI and renewed V1 visual
acceptance remain separate post-push evidence.

## Stage 3.2-R9 independent Tee interfaces, brace direction, and fastener presentation

R9 removes the prior physical-layout coupling between Tee Interface A and Interface B.
Each interface accepts independent positive row and bolt-line counts and retains its
own geometry, bolt paths, result, applicability, and interface fingerprint. A
single-row physical group is representable but remains explicitly
`CALCULATION_NOT_SUPPORTED` where the accepted multi-row method is unavailable; the
assembly cannot receive an ordinary PASS from that state.

The connected brace gains one exact signed planar inclination input from `-90°` to
`+90°`. Backend geometry rotates the brace and Interface A about the established Tee
contact normal using the deterministic R5 trigonometric authority. The Tee, supporting
member, Interface B, and global force/moment/reference remain fixed. Profile roll stays
a separate cross-section clocking input, and skew/out-of-plane direction remains future
controlled scope.

Tee bolts use the existing shared direct/reference solid-fastener renderer. Shank,
head, and nut follow the accepted presentation ratios; washers appear only when existing
geometry authority supplies them. R9 adds no resistance equation, demand distribution,
dependency, workflow, persistence, report, tag, or Stage 3.3 work. R8 hosted CI is
accepted four of four green. R9 hosted CI and V1-R9 through V5-R9 visual acceptance
remain separate post-push evidence.

## Stage 3.2-R10 fixed Tee grids and independent group positioning

R10 corrects Interface A so brace inclination rotates the brace/member profile but not
the Tee-stem bolt grid or bolt axes. Interface B remains fixed. Stable backend datums
own both interfaces, and each layout independently selects legacy edge-distance control
or signed in-plane vertical/horizontal group offsets. A mode conversion that represents
the same physical group preserves bolt centers exactly; no normal offset is available.

Exact server-authored complete-hole clearances distinguish geometry containment from
code edge-distance meaning. The controlled Angle boundary is invalid at `1.0 in` and
`1.2814 in`, exactly valid at `1.2815 in`, and valid at `1.3 in`. A fixed-grid load
outside the existing row-distribution frame proof remains visible as valid physical
geometry but returns `ROW_DISTRIBUTION_FRAME_COMPATIBILITY_NOT_PROVEN` and no ordinary
design result. R10 adds no equation, demand distribution, dependency, workflow,
persistence, report, tag, normal offset, or Stage 3.3 work. Hosted CI and V1-R10 through
V6-R10 visual acceptance remain post-push evidence.

## Stage 3.2-R12 connected-member fabrication end trim

R12 adds an explicit optional fabrication end trim to the connected Tee member. The
backend derives the Tee flange inner clearance plane from the physical connector,
translates a parallel cut plane by an exact nonnegative user clearance, clips the
actual Angle, Channel, W/I, RHS, or Flat Plate solids, and publishes deterministic
trimmed faces and scene meshes. Trim disabled preserves the complete R11 canonical
geometry and every controlled legacy fingerprint.

The same trimmed physical geometry owns interference and per-bolt center-to-trim and
hole-edge-to-trim clearances. Negative clearance, remaining interference, incomplete
hole containment, or a trim/resistance mapping that has not been evaluated fails
closed. No resistance equation, automatic bolt relocation, frontend engineering clip,
dependency, workflow, persistence, report, tag, or Stage 3.3 work is added. Hosted CI
and V1-R12 through V6-R12 visual acceptance remain direct post-push evidence.

## Stage 3.2-R13 Tee length anchoring and longitudinal position

R13 makes the existing finite Tee extrusion explicitly positionable along its existing
template longitudinal datum and axis. Exact `CENTER`, `POSITIVE_L_END`, and
`NEGATIVE_L_END` modes resolve the body center and both physical ends from one signed
position. Omitted fields preserve the legacy centered-zero geometry and every accepted
legacy fingerprint; physically identical anchor representations share one resulting
body identity.

The entire flange and stem move together. R10/R11 Interface A and B bolt centers and
axes remain fixed to their independent physical datums, while complete-hole clearances
to the moved Tee ends are recomputed from the actual finite body. Connected and
supporting member geometry, selected support face, R12 trim input/reference meaning,
and resistance authority are unchanged. R13 adds no equation, automatic bolt or trim
relocation, one-sided flange-width behavior, dependency, workflow, persistence, report,
tag, or Stage 3.3 work. Hosted CI and V1-R13 through V7-R13 remain direct post-push
evidence.

## Stage 3.2-R14B region-specific FRP material bases

R14B corrects the shared backend source that previously assigned one planar material
orientation to every wall of a component. W/I web, Tee stem, Angle Leg 2, Channel web,
and both RHS side walls now receive exact physical-plane, thickness-consistent,
right-handed bases. Tee flange, W/I flanges, Angle Leg 1, Channel flanges, RHS top and
bottom, and Flat Plate retain their exact audited-correct bases.

The correction changes no geometry, material property value, directional lookup,
equation, capacity, utilization, status, applicability, or resistance authority.
Basis-derived preview/platform identities transition with exact provenance; unrelated
engineering/interface/result identities remain exact. The frontend continues to render
only backend-authored vectors and adds physical-region labels. R14/R14A were
diagnosis-only stops. Hosted CI and V1-R14B through V7-R14B remain direct evidence;
Stage 3.3 is not started.

## Stage 3.2-R14C region-embedded material-axis visualization

R14C replaces normal-mode floating material triads with one shared region-embedded
presentation used by direct, multi-row, and Tee scenes. Exact component/element/region
identity associates each backend R14B basis with its existing box or trimmed-mesh
primitive. LW/CW use bounded bidirectional in-plane axes; TT uses a surface-normal
dot/cross/tangent marker updated from camera position through direct refs. One compact
accessible legend appears only while the existing material-axis overlay is enabled.

Presentation anchors use oriented primitive bounds and a small positive-TT display
offset. They do not mutate snapshots or engineering geometry. R14C changes no backend,
API, material basis, property, equation, demand, resistance, status, applicability,
fingerprint, dependency, workflow, controlled engineering artifact, or freeze tag.
Hosted CI and V1-R14C through V8-R14C remain direct post-push evidence; Stage 3.3 is
not started.

## Stage 3.2 Tee-connection freeze

Stage 3.2 closes with a governance/reproducibility freeze over the accepted Tee
connection baseline. The machine-readable manifest freezes source and dependency Git
objects, every accepted Stage 3.2 controlled artifact, inherited registered Stage 2
hashes, approved fingerprint transitions, behavioral contracts, accepted CI/visual
evidence, and explicit unsupported boundaries. The tagged governance state contains
the manifest while identifying the earlier accepted product commit separately.

This freeze adds no connection family, engineering method, production behavior,
dependency, persistence, report, or workflow. Stage 3.3 and every other connection
family remain unstarted after the freeze.

## Stage 3.3A single FRP clip-angle connector

Stage 3.3A implements one single-angle connection between a supported FRP member and
a selected W-column or W-beam flange. The slice owns two independent physical bolt
groups, exact finite-leg/end containment, region-specific material bases, fixed-plane
member trim, semantic hand mirroring, backend-authored preview geometry, explicit
design execution, and fail-closed normal-action and connector-body limitations.

The slice reuses the frozen Stage 3.2 shared platform and existing verified demand and
resistance engines; it adds no new equation, prying model, persistence, report, or
dependency. Planned Stage 3.3B adds paired/double clip angles under a future controlled
authority. Planned Stage 3.3C expands support selection to W webs and beam-to-beam
arrangements. Neither future stage is started here.

## Stage 3.3A-R1 connected-profile preview binding correction

R1 repairs the complete connected-member preview matrix without expanding connection
family scope. The clip-angle backend now places and serializes the selected Flat Plate,
Angle, Channel, W/I, or RHS profile for both Brace and Beam roles through the existing
standard-shape and placed-component visualization authorities. The shared Tee adapter is
extracted without changing its behavior, geometry, results, or fingerprints.

The frontend maps backend primitives and region axes only. Profile edits retain the
existing debounced, abortable, latest-response-wins preview boundary; no resistance is
run until the explicit design action. Stage 3.3B remains planned and unstarted.

## Stage 3.3A-R2 connected-member surface-side placement correction

R2 corrects Interface A at the backend physical-placement boundary. The selected Flat
Plate, Angle, Channel, W/I, or RHS surface point is retained while its outward normal is
aligned with the fixed connector contact direction. This places connected-member
material entirely behind the contact plane and the clip-angle connected leg entirely
in front of it, with exact face coincidence and no profile-family display offsets.

An exact oriented-box separating-axis check permits surface contact but rejects positive-
volume member/connector overlap. Finite-clear-region failures remain visible as invalid
geometry, including a W/I web whose flange junctions cannot contain the finite clip leg.
The support leg/contact, connector heel/body, bolt grids and paths, demands, equations,
results, and all controlled Stage 3.3A fingerprints are unchanged. No frontend
production, dependency, workflow, frozen Tee, or tag change is made. Stage 3.3B remains
planned and unstarted.

## Stage 3.3B symmetric paired clip-angle connector

Stage 3.3B implements only the approved symmetric pure-reaction-shear pair. A fixed
right-handed pair frame owns exact mirror geometry, one common three-layer through-bolt
group, two mirrored support groups, action-symmetry proof, exact branch wrench recovery,
controlled per-layer demand allocation, and three exact-once calls through the existing
demand/resistance engines. The unified backend-authored preview and frontend workspace
retain explicit preview/design separation and the accepted Stage 3.3A/Tee behavior.

Future controlled scope includes asymmetric paired angles, independent left/right
support groups, support-web and beam-to-beam arrangements, pair/individual-angle body
resistance, and common-bolt double-shear resistance. Stage 3.3C and every such extension
remain unstarted.

## Stage 3.3C1 shared rectangular-section/full-through-bolt core

Stage 3.3C1 establishes dormant shared architecture for later connection-family
expansion. It adds a real Solid Rectangular Section distinct from RHS, exact Y+/Y-/Z+/
Z- opposing exterior faces for both rectangular families, and exactly seven shared
support targets. The physical bolt path now distinguishes `MATERIAL_LAYER` from
`FREE_SHANK_SPAN`: an RHS path is near wall, cavity, and far wall on one continuous
external-head-to-external-nut bolt; an SRS path is one continuous material layer.

Existing resistance handoffs still consume material layers only. No API route,
production selector, frontend source, resistance equation, ordinary unsupported PASS,
or accepted Direct/Tee/single-angle/paired-angle fingerprint changes. Historical R8
same-wall RHS behavior remains backward-compatible. Stage 3.3C2 integrates Tee and
Single Clip-Angle support-profile expansion; Stage 3.3C3 integrates Paired Clip-Angle
connected/support expansion. For every compatible rectangular-section
selector introduced later, an RHS option requires an SRS counterpart.

## Stage 3.3C2 Tee and Single Clip-Angle rectangular integration

Stage 3.3C2 activates the C1 core only for Tee and Single Clip-Angle. One shared
contract/editor owns exactly seven supporting targets, while connected RHS choices gain
matching SRS choices. Open profiles use their actual selected web or angle leg. RHS and
SRS use backend-authored opposing-face paths, external-only hardware, exact two-face
containment, and one physical bolt per path.

Only affected historical Tee and Single-Angle RHS identities are controlled successors.
Non-RHS Tee/Single, all Paired-Angle and Direct behavior, equations, dependencies,
workflows, and both freeze tags remain unchanged. Unsupported rectangular local
mechanics prevent ordinary PASS through explicit design limitations but do not invalidate
sound geometry. Stage 3.3C3 completes the separately controlled paired-angle expansion
below.

### Stage 3.3C2-R1 preview integration correction

R1 restores the already-authorized C2 behavior at two narrow application boundaries.
Only current `3.3C2-RC1` Tee RHS requests omit the historical single-wall layer so the
accepted full-through composer owns the Tee stem, near wall, cavity, far wall, two-face
containment, and external hardware. Single Clip-Angle results now preserve the accepted
request contract version instead of replacing it with the historical A contract.

No geometry rule, path meaning, resistance equation, fingerprint input, frontend
production source, dependency, workflow, or freeze identity changes. Stage 3.3C3 is
governed by the separate authority recorded below.

## Stage 3.3C3 Paired Clip-Angle profile/support completion

Stage 3.3C3 activates the accepted C1 full-through core and C2 shared-support contract
for the symmetric Paired Clip-Angle workflow. Connected members now cover exactly Flat
Plate, W/I, Channel, Angle, RHS, and SRS. Supporting members use the exact seven-target
shared registry/editor, including open W/Channel/Angle surfaces and RHS/SRS rectangular
full-through targets, with no W Beam Web target.

The backend remains geometry and path authority. One common RHS/SRS bolt crosses the
positive connector, full connected section, and negative connector; each rectangular
support group owns a separate connector/profile full-through bolt. Open-profile paths
use exact selected surfaces and complete-hole containment. The accepted symmetry proof
may decline equal sharing for Angle or open-support topologies while retaining valid
geometry and an explicit `NOT_EVALUATED` distribution state. No new sharing or local
rectangular resistance equation is added. Legacy Stage 3.3B, Tee, Single-Angle, Direct,
dependencies, workflows, and both freeze tags remain exact. A future Clip-Angle freeze
requires a separate order.

## Stage 3.3 Clip-Angle family accepted-baseline freeze

Stage 3.3 is closed by a governance-only reproducibility freeze over the accepted
Single and Symmetric Paired Clip-Angle families and their shared rectangular/full-through
core. Product baseline `a15f6f9bc820bc7e2d466db8519bcb9582b96e55`, GitHub Actions
run 69, and the user's final visual acceptance are the acceptance evidence. The freeze
manifest and audit preserve exact production source, controlled authority, dependency,
workflow, tag, fingerprint, supported behavior, and deliberate limitation identities.

The next major connection family requires a separate controlling order. It may not
silently alter the frozen Stage 3.3 contracts; any intentional change must identify the
superseded behavior, add controlled engineering authority when meaning changes, record
exact before/after fingerprints, prove unaffected Stage 3.3 regressions, and retain all
historical freeze/tag evidence.

## Stage 3.4A Multi-Member Tee node

The owner-selected next family is a separate Multi-Member Tee node. The superseded
Gusset-Plate draft is unapproved and is not registered or implemented. One finite Tee
may carry an optional Upper Angle Brace, horizontal W/I Beam, and Lower Angle Brace on
the same stem face. Every active slot owns its profile, placement, trim, bolt group,
complete member-end wrench, demand call, result, and fingerprint; disabled slots are
absent rather than hidden.

The backend sums forces and shifts every moment to one support reference using the
exact `M + r × F` relationship, then calls the accepted Stage 2.5A demand seam once per
active slot and once for the support group. Preview executes zero resistance equations,
and design remains explicit. Tee-body and intergroup load-path/stability methods are
not authorized, remain visible `NOT_EVALUATED`, and prohibit ordinary PASS. The frozen
single-member Tee and Clip-Angle families remain exact.

## Stage 3.4B Multi-Member Tee profile/support expansion

The controlled Stage 3.4B successor keeps the historical `3.4A-RC1` default exact and
adds `3.4B-RC1`. Each Upper, horizontal Middle, and Lower slot may independently use
Flat Plate, Angle, Channel, W/I, RHS, or SRS; the Middle Flat Plate and Angle cases are
explicit product requirements. One shared profile contract/editor owns family-specific
fields and selected physical surfaces. One existing shared support registry/editor owns
the exact seven approved targets, with no W Beam Web target.

The implementation composes accepted physical profile, trim, full-through-bolt,
support-target, material-axis, demand, and visualization infrastructure. RHS cavities
remain free-shank spans and SRS remains one solid volume. Profile/support choices do not
alter slot actions or the exact assembled support-transfer wrench. Unsupported Tee-body,
intergroup, and rectangular local mechanics stay visible design limitations; they do not
become geometry errors or authorize ordinary PASS. No demand or resistance equation is
added. After hosted CI and direct visual acceptance, a separate Multi-Member Tee family
freeze is recommended. No Stage 3.4C engineering-method stage is currently selected.

## Stage 3.4 Multi-Member Tee family freeze

The accepted Stage 3.4 product baseline is
`b2da06ea04276e2113906d4e7c2b4295496f685f`. The complete Stage 3.4A, successor-safe
R1, workspace R2, and profile/support Stage 3.4B chain is frozen through one
machine-readable governance manifest and a successor-safe audit. Accepted GitHub
Actions runs 72, 73, and 74 are four of four green, and direct owner visual acceptance
is dated 2026-08-28.

This is governance/reproducibility only. Production source, controlled engineering
artifacts, dependencies, workflows, equations, geometry, results, statuses, and
fingerprints do not change. Historical Stage 2.3, Stage 3.2, and Stage 3.3 freeze tags
remain immutable. No Stage 3.4C or next major connection family is selected by this
freeze.

## Stage 3.5A — Beam-to-Concrete Wall using symmetric paired FRP clip angles

Stage 3.5 selects the concrete-support shear family. Stage 3.5A implements its first
bounded slice as a separate product type: one horizontal W/I beam, a symmetric pair of
FRP clip angles, one common beam group, two mirrored wall-anchor groups, and a finite
concrete wall. The only user action is signed vertical reaction shear; no user moment
is accepted.

The backend is authoritative for wall/beam/connector geometry, branch equilibrium,
eccentricity-induced moments, and the deterministic external anchor-design handoff.
No concrete or anchor capacity is calculated, no new demand or resistance equation is
introduced, and ordinary whole-connection PASS remains unavailable while external
anchor/concrete design is outstanding. All prior frozen connection families remain
exact. Stage 3.5B was subsequently selected and implemented under its own controlled
direct side-lap wall contract.

## Stage 3.5A-R1 — Concrete-Wall Paired-Angle completion

The controlled successor `3.5A-R1-RC1` preserves the historical `3.5A-RC1` W/I and
2×2 case exactly, then completes the product's presentation and physical scene. Shared
shear-family labels use Brace/beam terminology, the connected panel is role-neutral,
and the accepted six-profile paired-angle registry supplies Flat Plate, Angle, Channel,
W/I, RHS, and SRS geometry, selected surfaces, roll, material regions, and physical bolt
paths. Both clip-angle owners and the connected member must always be present.

Each mirrored wall group defaults to 1×1 at the existing centroid. The exact branch and
combined wall wrenches do not change. Because one anchor cannot equilibrate the retained
branch moment, the default exports no invented per-anchor solution and instead records
external design required. Existing 2×1 and 2×2 patterns remain available and receive a
nominal internal trace only where equilibrium is applicable. No concrete/anchor
capacity, new equation, freeze-tag transition, or Stage 3.5B implementation is included.

## Stage 3.5A-R2 — Material axes and three-component force

The controlled `3.5A-R2-RC1` successor preserves the complete Stage 3.5A/R1 product
and expands the user action to signed Major shear (`V_W`), Minor shear (`H_W`), and
Axial force (`N_W`). User moments remain zero and unavailable. Exact wall-reference
translation retains all eccentricity-induced moments, and external handoff has distinct
branch-resolved and combined-layout modes. Minor shear receives no fabricated paired
branch distribution or bolt-tension/prying response; axial wall-leg and nonmajor-force
qualification limitations remain visible and fail closed.

The material-axis correction is presentation-only. Backend R14B bases remain exact for
all six connected profiles and both clip angles; the renderer now creates TT markers from
the authoritative TT normal without trying to encode reflected triads as quaternions.
No equation, dependency, workflow, freeze tag, or historical fingerprint changes. Stage
3.5B remains unstarted.

## Stage 3.5B — Direct side-lap Angle/Channel to concrete wall

Stage 3.5B adds `3.5B-RC1` as a separate Brace/beam connection and leaves every
Stage 3.5A/R1/R2 contract intact. A finite Channel web or selected Angle leg overlaps a
finite wall behind its free end while the member projects beyond the wall. The direct
external anchors penetrate only the selected FRP region and concrete embedment; no
intermediate connector or fictitious far-side hardware exists.

The engineer controls side-lap length and anchor position independently. Ordinary lap
editing never moves anchors, and the explicit center action is the only automatic
repositioning command. The accepted Axial/Major/Minor force state is translated exactly
to the real anchor centroid with zero user moment. Existing applicable local FRP methods
may execute only on explicit design; unsupported normal/out-of-plane behavior and all
anchor/concrete capacity remain external. No new equation, dependency, workflow, tag,
column-base product, persistence, or report is introduced.

## Stage 3.5C — Column base using single/double web angles to concrete

Stage 3.5C adds the separate strict `3.5C-RC1` Column connection. One vertical
W/I column bears over a finite concrete base through one web angle or an exactly
symmetric pair. The backend authors the `S_C/T_C/L_C` base frame, full column
section, angle solids and bearing footprints, web groups, blind anchor groups,
material-region bases, and exact component/foundation handoff.

The column web receives 100% of compression `Pu` as an `LW` local-design demand;
the base-angle system independently receives 100% as vertical-leg `CW`. Exact
pair symmetry is required before each Double branch receives `Pu/2`. This is a
serial/conservative component-design allocation, not two physical reactions, so
the foundation equilibrium records `Pu` once. The same physical group action may
therefore classify differently against the independent web and angle material bases.

Compression and the two controlled shear inputs are accepted; uplift and user
moments are excluded. No angle-body/heel compression capacity, concrete or anchor
capacity, web-normal bolt tension/prying response, or new demand/resistance equation
is introduced. Unsupported items remain explicit `NOT_EVALUATED` or
`EXTERNAL_DESIGN_REQUIRED` boundaries and prohibit ordinary PASS. Preview remains
resistance-free and design remains explicit. Historical Stage 3.5 products and all
frozen families remain exact; the beam web-splice family is not started.

## Stage 3.5C-R2 — Signed axial compression/uplift expansion

Stage 3.5C-R2 adds strict successor `3.5C-R2-RC1` while retaining historical
`3.5C-RC1` exactly. The current workspace accepts one signed axial force (`+` uplift,
`-` compression), signed web-plane shear, signed web-normal shear, and no user moments.
The default `-20 kip` case reproduces the historical physical result.

The backend preserves the exact signed local action through demand/applicability mapping,
assigns 100% to column-web `LW` and independently 100% to angle-system vertical-leg `CW`,
and permits signed 50/50 Double branches only after exact symmetry. Foundation equilibrium
contains the action once. Reverse paths without accepted authority remain `NOT_EVALUATED`;
uplift angle body/heel and prying are not calculated, and anchor/concrete uplift remains
external. No new equation, dependency, workflow, persistence, report, or beam web-splice
implementation is included. GitHub Actions run #83 passed 4/4 and the owner accepted the
final Stage 3.5C-R2 visual result on 2026-08-29.

## Stage 3.5F — Concrete-Support Shear Family freeze

The Stage 3.5 family is frozen at accepted product baseline
`4c154b9d31e2a0100fd9f8c5cc90243e838a19c7`. This governance-only transition records
the exact Stage 3.5A/R1/R2, 3.5B/R1, and 3.5C/R1/R2 chain, accepted CI runs #76–#83,
owner visual acceptance, controlled and inherited authorities, engineering fingerprints,
source/dependency/workflow identities, external-design boundaries, and successor policy.

Production behavior, calculation methods, controlled engineering artifacts, dependencies,
and workflows do not change. The successor-safe audit supports tag, explicit historical
object, and `manifest_only` resolution without ever substituting successor `HEAD`.
Beam-to-beam web splice remains planned only; it has not begun and this roadmap entry
creates no design authority for it.

## Stage 3.6A — Symmetric Double Web Splice Plates

Stage 3.6A starts the Beam Splice family with one bounded symmetric topology: two
locked-identical collinear W/I beams, a positive end gap, two locked-identical opposing
FRP web plates, and separate mirrored left/right Plate/Web/Plate through-bolt groups.
The strict `3.6A-RC1` request accepts force transfer only and no user moment. Backend
geometry and eccentric group-demand resolution preserve equal-and-opposite Beam A/Beam B
actions, all group-centroid moments, actual per-layer material directions, and an
independent reversed Beam B failure path.

This stage adds no resistance equation. The existing local handoff is used only where
already authorized. Plate inter-group body transfer, common-bolt double shear,
minor-shear bolt-axis/prying response, flange-splice behavior, and single-plate behavior
remain explicit limitations. The Stage 3.5 freeze and all earlier frozen families remain
immutable; hosted CI and owner visual acceptance remain separate acceptance gates.

## Calculation Slice 4 — Chapter 7 Plate Strength prerequisite

This backend-only prerequisite provides normative pure-mode Chapter 7 FRP plate tension,
longitudinal compression rupture/buckling, bounded combined-compression buckling, and
in-plane shear rupture/buckling. It reuses the existing adjusted-property, factor,
Decimal, provenance, and fingerprint contracts and introduces no frontend product stage.

The revised Stage 3.6B rational web-splice completion remains paused until this slice is
separately accepted. Slice 4 does not alter or automatically wire into `3.6A-RC1`, does
not implement rational plate-body interaction or common-bolt double shear, and preserves
the Stage 3.5 and earlier family freezes exactly.

## Stage 3.6B RC2 — Web-Splice Resistance Completion

Stage 3.6B RC2 resumes the accepted web-splice successor under strict `3.6B-RC2`.
It derives the finite clear body between the two physical bolt groups, retains the exact
eccentric body moment, evaluates left/joint/right critical sections, and consumes the
unchanged Calculation Slice 4 pure-mode tension, compression/buckling, and shear results.
The controlled linear normal/shear interaction is a rational project method requiring
Section 2.3.2 qualification, an engineer-of-record review, and report disclaimer; its
successful terminal state is review-qualified rather than ordinary PASS.

The two locked Plate/Web/Plate paths prove two physical shear planes per bolt. Each
metallic check consumes the actual Stage 2.5A bolt vector and source/thread authority;
default F593 strength remains pending and is never inferred. Preview executes no
resistance, design remains explicit, and historical `3.6A-RC1` remains exact.
Minor-shear bolt-axis response, user moment, new equations, dependencies, workflows,
persistence, reports, and another family remain excluded. GitHub Actions run #87 passed
all four hosted jobs in approximately 4m30s, and the owner directly accepted the final
W/I product behavior on 2026-08-30.

## Stage 3.6F — W/I Web-Splice Family freeze

Stage 3.6F freezes accepted product baseline
`5f77abd0eeaf61718756e9962ae3a5a67d6db528` as a governance/reproducibility record.
The immutable W/I-only scope consolidates Stage 3.6A topology/demand, Calculation Slice 4
pure-mode Chapter 7 authority, and Stage 3.6B RC2 rational body/double-shear behavior,
including qualification, disclaimer, result precedence, and deliberate limitations.

Production source, engineering methods, controlled engineering artifacts, dependencies,
workflows, results, and fingerprints do not change. The successor-safe audit resolves by
historical tag, explicit object, or `manifest_only` and never substitutes successor
`HEAD`. All Stage 2.3, 3.2, 3.3, 3.4, and 3.5 freeze tags remain immutable.

`CHANNEL_WEB_SPLICE = NOT_IN_STAGE_3_6_FROZEN_SCOPE`. A Channel member's centroid/
reference axis is generally offset from its web plane, so a future Channel successor
requires explicit reference-axis/eccentricity and transfer-moment authority plus frozen
Stage 3.6 regression evidence. Channel support and every later stage remain unstarted.

## Stage 3.7A — Column-Base Profile Matrix Expansion

Stage 3.7A is the controlled additive successor to the frozen Stage 3.5 column-base
product. Strict `3.7A-RC1` supports W/I, RHS, SRS, and Angle columns with Single and
Double base-angle assemblies through one existing Column workspace and the existing
stateless preview/design endpoints.

The backend owns each profile frame, actual member reference, contact/opposite faces,
physical bolt path, layer stack, action shifts, demand provenance, limitations, handoff,
material axes, and visualization. Rectangular profiles reuse the accepted full-through
core; Angle Double is limited to opposite broad faces of one selected leg. The serial
column-full/base-angle-system-full demand philosophy and one foundation reaction remain
exact, and equal branch allocation requires the controlled proof.

No new resistance equation, concrete/anchor capacity, prying response, dependency,
workflow, persistence, report, or tag is added. A different-leg Angle moment topology
is planned only and requires separate future authority; it is not begun here.

## Stage 3.7F — Column-Base Shear Family freeze

Stage 3.7F freezes accepted product baseline
`0403bc8a4ace0df95b45a84a55832c17b18d6008` without changing product behavior. The
immutable scope is W/I, RHS, SRS, and Angle columns crossed with Single and Double
base-angle assemblies, including the R1 negative-face Angle correction, actual Angle
centroid/reference eccentricity, generated moments, serial component demands, one
foundation reaction, current applicability, external handoff, and visualization state.

The deterministic manifest and tag/object/`manifest_only` audit preserve all earlier
family freezes and make source, package, lock, workflow, controlled-artifact, and
fingerprint identities independently reproducible in a depth-one tagless checkout.
Production, engineering methods, dependencies, and workflows remain unchanged.

The different-leg Angle-column base is a future moment-couple topology and is explicitly
not part of Stage 3.7. This freeze does not begin it or any other later family.

## Calculation Slice 5 — W/I Major-Axis Moment Component Resultants

The Moment Connections program begins with a backend-only prerequisite under strict
contract `CS5-RC1`. It reuses the accepted W/I geometry and exact Decimal architecture
to decompose axial force, major shear, and major-axis moment into complete top-flange,
web, and bottom-flange resultants while preserving local region moments, web shear and
moment participation, and exact equilibrium.

The exact flange couple, residual web moment, stress extrema, and full-moment `M/z`
reference are diagnostic outputs. `M/z` is not the sole controlling component demand.
Moment-connection stiffness and qualification, physical splice geometry, resistance,
API/frontend exposure, and Stage 4.1A remain pending separate authority. This
calculation slice changes no frozen family or historical fingerprint.

## Stage 4.1A — W/I Major-Axis Moment Splice

Status: Implemented under controlled contract `4.1A-RC1`; local and isolated gates,
publication, hosted CI, and owner visual acceptance are recorded separately.

This stage accepts Calculation Slice 5 and adds only
`WI_BEAM_MAJOR_AXIS_MOMENT_SPLICE`. The physical topology is two identical W/I beams,
a positive gap, the frozen Stage 3.6 two-plate web subsystem through a successor adapter,
and a balanced flange system with one full-width outer plate plus two symmetric inner
strips on each flange. Complete Slice 5 component wrenches, including flange/web local
moments and web shear, remain authoritative. `M/z` is reference-only.

Exact outer/inner force-line equilibrium, rational flange-face sublayers, Calculation
Slice 4 plate-body strengths, and actual unequal two-plane common-bolt demands are
implemented under mandatory rational-method engineering review and Section 2.3.2
qualification. Ordinary prescriptive PASS, bolt-axis tension/prying, stiffness,
rotation capacity, and full-strength classification are not claimed. Dependencies,
workflows, tags, and every frozen historical fingerprint remain unchanged.

Channel moment-splice support is reserved for future Stage 4.1B and requires its own
controlled reference-axis, eccentricity, geometry, and engineering authority.

## Stage 4.1A-F — W/I Major-Axis Moment Splice freeze

Status: governance/reproducibility freeze of accepted baseline
`59713e53c522a6a301e19e458559986fb114d735`.

Calculation Slice 5 and the `4.1A-RC1` physical W/I moment splice are frozen together.
The immutable scope retains complete region wrenches, web shear and free moment,
balanced outer/inner flange force lines, one outer plus two split-inner plates per
flange, rational face sublayers, actual unequal two-plane bolt demand, local/body
checks, exact equilibrium, mandatory review/qualification, preview/design separation,
source-pending F593 handling, visualization, and request-state behavior.

The deterministic manifest and tag/object/`manifest_only` audit preserve exact source,
artifact, package, lock, workflow, fingerprint, and historical-tag identities without
changing production or engineering behavior. Stage 4.1B Channel Moment Splice remains
a future separately controlled family and was not begun by this freeze.

## Calculation Slice 6 — Channel moment references and component resultants

Status: backend prerequisite implemented under controlled contract `CS6-RC1`; physical
Stage 4.1B remains pending separate authority.

This slice establishes the Channel member-centroid, web-plane, and shear-center reference
system before any Channel moment-splice geometry is created. It supports the controlled
rational median shear-center source and explicit qualified source, carries the exact
web-plane/shear-center eccentricities, and retains every moment and torsion generated by
reference translation.

The engine returns complete top-flange, web, and bottom-flange force/moment resultants for
all six action components, exact member-reference equilibrium, stress/couple/residual
diagnostics, and deterministic U.S./SI identities. It adds no resistance, API route,
frontend production behavior, dependency, workflow, tag, or frozen-family transition.
Physical Channel splice plates, bolts, local checks, resistance, qualification, and UI
remain future Stage 4.1B work.

## Stage 4.1B — Channel Major-Axis Moment Splice

Status: implemented under controlled successor contract `4.1B-RC1`.

This stage adds `CHANNEL_BEAM_MAJOR_AXIS_MOMENT_SPLICE` to Moment Connections without
changing frozen W/I behavior. It consumes Calculation Slice 6 exactly once per request,
preserves centroid/shear-center references and generated torsion, and implements two
web plates plus four flange plates with backend-authoritative paths and material axes.

The rational back/opening web-face couple closes exact normal, shear, local-moment, and
free-torsion equilibrium without assuming equal web-face shear. Existing local FRP,
Chapter 7 plate-body, and unequal common-bolt plane engines are reused. Preview/design
separation, source-pending F593 behavior, engineering-review disclaimer, and Section
2.3.2 qualification remain mandatory. Warping, torsional resistance, prying, stiffness,
rotation capacity, and any later moment family are outside this stage.

## Stage 4.1 Beam Moment Splice Family freeze

Status: accepted W/I + Channel product baseline `6d953dbef648bd35ab21b5208432579cdeb59902`
is frozen by the scope-specific family manifest. Calculation Slices 5/6, Stage 4.1A,
Stage 4.1B, and the R1 runtime correction are included. The controlling order closes
owner visual acceptance and records accepted CI #93–#97; Slice 5 #92 is retained from
the existing W/I freeze record. Family governance-commit CI is separate and pending
direct four-job evidence after publication.

The new `stage-4.1-beam-moment-splice-family-freeze` annotated tag supplements, never
replaces, the eight historical freezes. Complete wrenches, Channel centroid/shear-center
and generated torsion, unequal face/bolt demands, rational review and qualification,
source-pending behavior, and all current limitations remain exact. This is zero-production
governance work. No next moment family is authorized or begun. Any successor needs its
own order, impact analysis, frozen regression, and an explicitly authorized fingerprint
transition; neither historical manifests nor tags may be moved or silently reinterpreted.

## Stage 4.2 RC1-R7 — W/I beam-to-wall moment successor

Authorized from accepted Slice 8 stabilization commit `473a3c8cd43f13022d254dae2477084895478c74` (106 commits). This separate physical product uses four FRP angles, complete Slice 5 region wrenches, Slice 7 neutral core/FRP provider, native Stage 2.5A flange demand, and Slice 8 web demand. Existing frozen products stay exact. The R7 default internal result is FAIL with native first-row governance; qualified attachment/connector and external wall design boundaries remain open. Full QA, browser, isolated verification, hosted four-job CI and owner visual acceptance are distinct gates. No 316SS or next family begins.

## Stage 4.2 accepted product freeze

The following is the historical Stage 4.2 freeze scope, not a prohibition on a later separately approved successor.

Status: FROZEN subject to the governance publication/tag gates in `docs/qa/STAGE_4_2_FREEZE.md`. Accepted product and correction `1dee177a02bcaec713c1e3f597b053790ff7337e` (108 commits), implementation `2886daab9d736926ba78fc9c6c44c652321756d1`, hosted #103 attempt 1 four jobs SUCCESS, and owner final visual/result acceptance are recorded. Original authority plus cumulative R3–R7 and only RC1-R7 G1–G128 remain controlling. The new scope-specific manifest pins SHA-256 `71A73CF5599B9A2540FC3DD22D024D99565E3D5A9FCF07FACC027D523257F997`. This governance-only successor changes no production or existing freeze. Concrete/anchor capacity stays external. 316SS and Stages 4.3/4.4/4.5 remain future, unfrozen and not begun.

## Stage 4.3 — W/I beam to FRP support moment connection

Current separately authorized implementation: `4.3-RC1`, after accepted Stage 4.2 freeze governance `c8094293e6da49aa830b5801f49aae537cccae2b`. Five receiving support configurations are implemented: W/I flange, W/I web, hollow square, solid square and Channel web. Four FRP angles retain complete native beam/component/core wrenches. Actual support through-bolts and physical layers use qualified response evidence for normal/contact/prying and multi-wall/solid participation; no invented distribution or strength closes these boundaries.

The source-free defaults report native evaluated failures plus explicit missing qualifications. Numerical bolt/local-region adapters are exercised with isolated synthetic qualified fixtures, never supplied as production sources. Whole receiving-member analysis, stiffness, rotation capacity and full-strength classification remain external/not evaluated. The controlled 90-item matrix and 80 profile/load/unit cases are traced in the Stage 4.3 QA record. Publication requires complete isolated QA followed by direct hosted four-job evidence. Owner final visual/result acceptance remains pending; this stage is not frozen and authorizes neither 316SS nor a later family.

## Current Stage 4.3 freeze milestone

Accepted engineering implementation `2e4416a4dbd0b99c6a7280f6576264c37c09101a`; security successor `513e1c9150e63206e432a8d2f971617b6ed9c203`; current tests-only successor/baseline `36f6049c09b3df29ea899af826fbba19f26496b3` (112 commits). Engineering CI #105 attempt 2, security CI #106 attempt 2, and baseline CI #107 attempt 1 are accepted four-job SUCCESS. Owner final visual/result acceptance is ACCEPTED.

Manifest: `docs/governance/STAGE_4_3_WI_BEAM_FRP_SUPPORT_MOMENT_CONNECTION_FREEZE_MANIFEST.json`, SHA-256 `F120A681C4F85B36EA7519F46E16567F4913C5A181D1BDD6AF07F6CA1E6CC7F0`. See `docs/qa/STAGE_4_3_FREEZE.md`. Status FROZEN activates only after complete governance QA, fresh object-isolated verification, normal main push, direct governance CI 4/4, and verified annotated tag `stage-4.3-wi-beam-frp-support-moment-connection-freeze`. Post-commit evidence is external; SELF is the governance commit with parent `c59ead41acf7e2507842de86fca2a578ee8cb8e3` and expected count 114.

All five receiving configurations (W/I flange, W/I web, hollow square, solid square, Channel web) remain exact. Complete native dependency wrenches, material directions, actual through-bolt paths, native failure/source traces and qualified support-response boundaries are unchanged. In-plane projection does not establish normal tension/contact/prying response; unavailable is not zero. Hollow walls do not automatically share load or confer double-shear capacity; full solid depth is not a thin plate. Whole receiving-member design remains `NOT_EVALUATED_CONNECTION_CONTRIBUTION_ONLY`. No ordinary PASS, new qualified source, Stage 4.4/4.5, or 316SS authority is created.

The next family is not started by this milestone. Stage 4.4, Stage 4.5 and 316SS remain future/unfrozen scope requiring separate controlled authority. Earlier implementation/pending entries remain historical rather than silently rewritten.

## Windows CI stabilization and final freeze target

Finalization supersedes only the prior freeze publication target: first governance commit `c59ead41acf7e2507842de86fca2a578ee8cb8e3` (count 113) had CI #108 attempt 2 with three green jobs and two Windows 5000 ms frontend timeouts. Final SELF has parent `c59ead41acf7e2507842de86fca2a578ee8cb8e3`, exact subject `test: stabilize Windows Stage 4.3 freeze timeouts`, and count 114. Only the two named tests receive local 15000 ms limits; `backend/tests/calculation/test_scope_boundaries.py` authenticates their exact timeout-only successors while preserving the original historical digest and tamper detection. Both tests passed 10 consecutive Windows runs. Production, engineering, controlled artifacts, dependencies, workflows and all ten existing tags remain unchanged. The final annotated freeze tag targets SELF only after complete local/object-isolated QA and direct hosted four-job SUCCESS; final run/tag evidence is recorded externally without amendment.

Finalization Order R1 SHA-256: `1397BC0FC17BD4C67D79FE3DFFB20A94818621FB091D309BD167DF81ADDF27F9`. Final manifest SHA-256: `F120A681C4F85B36EA7519F46E16567F4913C5A181D1BDD6AF07F6CA1E6CC7F0`.

## Stage 4.4 accepted scope and freeze boundary

Engineering implementation `e8bc2355bcecaabd70d31332cf1af3ee6eb17712` (115); accepted preview/state correction `d11012e771556f0ed5cac46f39ab40cb0a53273e` (116). Owner acceptance: **Stage 4.4 visual accepted**. Product CI #111 attempt 1 is four-job SUCCESS. The bounded tests-only scope-guard successor `388083ba0ae26be5de6bea986c668252db56adbb` (117) passed complete local/object-isolated QA and CI #112 attempt 1, four-job SUCCESS.

Manifest: `docs/governance/STAGE_4_4_ANGLE_COLUMN_TWO_LEG_MOMENT_BASE_FREEZE_MANIFEST.json`, SHA-256 `2035E881712086643440D67A4F78D142886F749F90BCEA285B01A2E7E37C9443`. Freeze order SHA-256: `21FA8D17DCBBDE6ED6BA2C93892D5343347E5FE521EB2ACB98578E89F86A8836`. See `docs/qa/STAGE_4_4_FREEZE.md`.

The completed scope is equal- and unequal-leg angle columns with exactly two base angles on different column legs, independent member/foundation hardware, signed N/Vx/Vy/Mx/My, required total foundation wrench including generated Mz, qualified branch/contact response and applicable inherited checks. Preview and explicit design remain separate; invalid current inputs retain a labeled LAST VALID scene without current action arrows and recover automatically when valid.

Future W/I/RHS/SRS biaxial bases, a 316SS provider, unqualified sharing/contact formulas, full anchor/concrete or column design, and stiffness/rotation/full-strength classification remain outside this freeze and are not started.

Freeze publication is gated: SELF is the governance commit with parent `388083ba0ae26be5de6bea986c668252db56adbb`, exact subject `chore: freeze Stage 4.4 angle-column two-leg moment base baseline`, and expected count 118. Stage 4.4 becomes FROZEN only after complete governance local/object-isolated QA, normal main push, direct governance four-job CI SUCCESS and verified annotated tag `stage-4.4-angle-column-two-leg-moment-base-freeze` (annotation: `Stage 4.4 angle-column two-leg moment base freeze`). Post-commit evidence records actual objects and run/job URLs externally without amendment or circular self-hashes.

## CME-2C C2-P2 isolated implementation

Additive internal clear-plate E3/F9/H2 provider from baseline `20fabefa90247dd3470d4698edda1b22d017e7c6` (128). See `docs/architecture/CME_2C_ISOLATED_CLEAR_PLATE_PROVIDER.md` for trusted pre-resolved frozen C2-M/P1 snapshots, the owner-approved raw-traced E3 Fy cap, native numerical authority, fail-closed boundaries and publication gates. Frozen production/tests and all fifteen tags remain unchanged; no public API, frontend, hardware or family material activation. One implementation commit is expected at count 129; exact-SHA hosted four-job verification is recorded externally after normal push. No new freeze tag or later-stage work.
